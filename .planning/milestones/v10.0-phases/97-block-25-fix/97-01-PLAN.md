---
phase: 97-block-25-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/series.h, src/rr_ids.h]
autonomous: true

must_haves:
  truths:
    - "Series addition succeeds when operands have different q_shifts"
    - "x1+x2 with theta2/theta3 quotients produces a valid aligned series"
    - "findpoly(x, x, 3, 3) on theta quotient sum returns polynomial relation (contains X)"
    - "maple-checklist Block 25 passes (no skip)"
    - "Existing findpoly and Block tests show no regressions"
  artifacts:
    - path: src/series.h
      provides: Series operator+ with q-shift alignment
      contains: "addAligned or alignment logic in operator+"
    - path: tests/maple-checklist.sh
      provides: Block 25 real test
      contains: "x1 + x2" and "findpoly(x, x, 3, 3)"
  key_links:
    - from: src/series.h
      to: Series::operator+
      via: alignment branch when q_shift != o.q_shift
      pattern: "addAligned|terms\[.*q_shift"
    - from: tests/maple-checklist.sh
      to: Block 25
      via: run "x1 := ..." "x2 := ..." "x := x1 + x2" "findpoly(x, x, 3, 3)"
      pattern: "Block 25"
---

<objective>
Modify Series operator+ to handle operands with different q_shifts by aligning terms on actual exponents before adding, so that x1+x2 (theta2²/theta2(q³)² + theta3²/theta3(q³)²) succeeds and findpoly(x,x,3,3) produces a polynomial relation. Block 25 passes without skip.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/series.h
@src/rr_ids.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify Series operator+ to support different q_shifts (align then add)</name>
  <files>src/series.h, src/rr_ids.h</files>
  <action>
1. Add a static helper `Series addAligned(const Series& a, const Series& b, int sign_b, int T)` in series.h that implements the alignment logic from addSeriesAligned in rr_ids.h:
   - Build std::map&lt;Frac, Frac&gt; terms; for each (n,v) in a.c: terms[a.q_shift + Frac(n)] += v
   - For each (n,v) in b.c: terms[b.q_shift + Frac(n)] += sign_b * v
   - Find min exponent among non-zero terms
   - Build output Series: out.q_shift = minExp, out.c[n] = terms[e] where n = (e - minExp) when expOffset is integer (den==1); skip fractional offsets
   - out.trunc = T; out.clean()
2. Modify Series::operator+ (lines ~160-178): when q_shift != o.q_shift and both have non-empty coeffs, replace the throw with:
   return addAligned(*this, o, 1, std::min(trunc, o.trunc));
3. Optionally refactor rr_ids.h addSeriesAligned to call Series::addAligned(s1, s2, c1_sign, T) to avoid duplication. If rr_ids.h does not include series.h transitively in a way that would create circular deps, prefer refactor. (rr_ids.h already includes series.h — no circular dep.)
4. Ensure operator- continues to work: it calls operator+(-o); -o has same q_shift as o, so (*this)+(-o) will hit the alignment path when this and o have different q_shifts — correct.
5. Handle edge cases: if one series is empty, use the other's q_shift (current behavior when same q_shift); addAligned should handle empty input by returning the non-empty one or zero.
  </action>
  <verify>
g++ -std=c++20 -O2 -o qseries main.cpp 2>&1 | head -20
echo 'x1 := theta2(q,100)^2/theta2(q^3,40)^2' | ./qseries 2>&1 | head -5
  </verify>
  <done>
Build succeeds; theta quotient expressions evaluate without "cannot add series with different q-shifts" error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Block 25 passes and run regression tests</name>
  <files>tests/maple-checklist.sh</files>
  <action>
1. Run Block 25 test: use the run() helper in maple-checklist.sh with commands:
   x1 := theta2(q,100)^2/theta2(q^3,40)^2
   x2 := theta3(q,100)^2/theta3(q^3,40)^2
   x := x1 + x2
   findpoly(x, x, 3, 3)
2. Pass criteria: output contains "X" (polynomial relation found)
3. Run full maple-checklist.sh and confirm Block 25 passes
4. Run acceptance-exercises.sh (Exercise 10 uses findpoly) and confirm EX-10a, EX-10b pass
5. Run tests/acceptance-rr-id.sh if it exists, and any other findpoly-related tests
6. If run-all.sh or acceptance-all exists, run it to catch regressions
  </action>
  <verify>
bash tests/maple-checklist.sh 2>&1 | grep -E "Block 25|pass|fail"
bash tests/acceptance-exercises.sh 2>&1 | grep -E "EX-10|pass|fail"
make 2>&1; make acceptance-all 2>&1 || true
  </verify>
  <done>
Block 25 passes; no regressions in findpoly or Block tests; acceptance-exercises EX-10 passes.
  </done>
</task>

</tasks>

<verification>
1. `x1 + x2` with theta2/theta3 quotients does not throw
2. `findpoly(x, x, 3, 3)` returns polynomial relation containing X
3. maple-checklist Block 25: pass
4. acceptance-exercises EX-10a, EX-10b: pass
5. Full regression: acceptance-all (or equivalent) passes
</verification>

<success_criteria>
- Series addition normalizes q_shift differences so findpoly receives aligned series
- findpoly(x, y, q, 3, 1, 60) on theta quotients produces polynomial relation (Block 25 uses findpoly(x,x,3,3))
- maple-checklist Block 25 passes (no skip)
- No regressions in existing findpoly or Block tests
</success_criteria>

<output>
After completion, create `.planning/phases/97-block-25-fix/97-01-SUMMARY.md`
</output>
