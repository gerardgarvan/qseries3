---
phase: 98-block-24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/maple-checklist.sh, maple_checklist.md]
autonomous: true

must_haves:
  truths:
    - "Block 24 is documented as N/A with explicit rationale (collect is Maple formatting-only)"
    - "maple-checklist Block 24 skip message includes clear rationale for N/A"
    - "maple_checklist.md Block 24 note references the N/A rationale"
    - "No regressions in maple-checklist or relation-finding Block tests"
  artifacts:
    - path: tests/maple-checklist.sh
      provides: Block 24 skip with explicit N/A rationale
      contains: "collect" and rationale text
    - path: maple_checklist.md
      provides: Block 24 N/A documentation
      contains: "Block 24" and rationale reference
  key_links:
    - from: tests/maple-checklist.sh
      to: maple_checklist.md
      via: shared rationale (collect is formatting-only; no computational need)
      pattern: "Block 24"
---

<objective>
Document Block 24 (collect) as N/A with explicit rationale. Maple's collect(%[1],[X[1]]) groups Watson modular equation terms by X₁ — a display/formatting function. Our formatRelation in repl.h produces flat "343X₂⁷+343X₂⁶+..." output that is mathematically equivalent. No code changes; only documentation updates to satisfy BLOCK24-01 (OR branch) and BLOCK24-02.

Purpose: Lower-effort path — relation output is already correct; collect is purely cosmetic.
Output: Updated maple-checklist.sh skip message, maple_checklist.md Block 24 note, verification that no regressions occur.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@maple_checklist.md
@tests/maple-checklist.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Block 24 skip with explicit N/A rationale</name>
  <files>tests/maple-checklist.sh, maple_checklist.md</files>
  <action>
1. In tests/maple-checklist.sh (lines 237-238): Replace the current skip message with one that includes an explicit N/A rationale. Change:
   # Block 24: collect(%[1],[X[1]]) — N/A (formatting function)
   skip "Block 24: collect — formatting only"
   To:
   # Block 24: collect(%[1],[X[1]]) — N/A
   # Rationale: collect is Maple formatting-only; relation output is mathematically equivalent; no computational need.
   skip "Block 24: collect — N/A (Maple formatting-only; output equivalent)"

2. In maple_checklist.md (Block 24 section, ~lines 244-249): Update the Block 24 note to reference the rationale. Change:
   - N/A — `collect()` is a formatting function
   To:
   - N/A — `collect()` is Maple formatting-only; relation output is mathematically equivalent; no computational need (see Phase 98 rationale).
  </action>
  <verify>
grep -n "Block 24" tests/maple-checklist.sh maple_checklist.md
  </verify>
  <done>
Both files contain the explicit N/A rationale. The skip message and checklist note are consistent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions in relation-finding and Block tests</name>
  <files>tests/maple-checklist.sh</files>
  <action>
1. Run maple-checklist.sh and confirm:
   - Block 23 (findnonhomcombo Watson) still passes
   - Block 24 is SKIP (not FAIL)
   - Block 25 (findpoly theta2/theta3) passes if Phase 97 is complete, or skip as before
   - Overall pass/fail/skip counts are acceptable (no new failures)

2. Run any relation-finding tests (acceptance-rr-id.sh, acceptance-exercises.sh) if they exist and confirm no failures

3. The only edits are comments and skip message text — no functional code changes, so regressions should not occur. Verification confirms the test harness still runs and Block 24 correctly reports SKIP with the new rationale.
  </action>
  <verify>
bash tests/maple-checklist.sh 2>&1 | grep -E "Block 23|Block 24|Block 25|PASS|FAIL|SKIP"
  </verify>
  <done>
maple-checklist runs successfully; Block 24 shows SKIP with rationale; no new failures in Block 23/25 or relation tests.
  </done>
</task>

</tasks>

<verification>
1. grep "Block 24" tests/maple-checklist.sh shows rationale in comment and skip message
2. maple_checklist.md Block 24 note includes "Phase 98 rationale"
3. bash tests/maple-checklist.sh — Block 24 SKIP, Block 23 pass, no regressions
4. acceptance-exercises.sh (if exists) passes
</verification>

<success_criteria>
- Block 24 documented as N/A with clear rationale (collect is Maple formatting-only; output equivalent; no computational need)
- maple-checklist Block 24 has explicit N/A rationale in the test file (skip message + comment)
- maple_checklist.md Block 24 references Phase 98 rationale
- No regressions in relation-finding or Block tests
</success_criteria>

<output>
After completion, create `.planning/phases/98-block-24/98-01-SUMMARY.md`
</output>
