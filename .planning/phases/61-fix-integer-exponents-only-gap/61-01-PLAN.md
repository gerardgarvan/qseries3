---
phase: 61-fix-integer-exponents-only-gap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/series.h
  - src/qfuncs.h
  - src/repl.h
autonomous: true

must_haves:
  truths:
    - "theta2(100) displays with q^(1/4) prefix"
    - "etamake(theta2(100),100) shows q^(1/4) in the output"
    - "Arithmetic on normal series (q_shift=0) is unchanged"
    - "Addition of series with different q_shifts throws error"
    - "Multiplication correctly adds q_shifts"
    - "All existing acceptance tests pass"
  artifacts:
    - path: "src/series.h"
      provides: "Series with q_shift field and propagation"
    - path: "src/qfuncs.h"
      provides: "theta2 sets q_shift = 1/4"
    - path: "src/repl.h"
      provides: "Display includes q_shift"
---

<objective>
Add rational q-power shift tracking to Series so theta2 can represent its q^(1/4) factor.

1. Add `Frac q_shift` field to Series (default 0)
2. Propagate q_shift through all arithmetic operations
3. Set theta2's q_shift to Frac(1,4)
4. Update display (str, formatEtamake) to show q_shift

Output: Modified `src/series.h`, `src/qfuncs.h`, `src/repl.h`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add q_shift to Series</name>
  <files>src/series.h</files>
  <action>
Add `Frac q_shift;` field to Series struct. Update:
- All factory methods: set q_shift = Frac(0)
- operator+/operator-: check q_shift match, throw if different, copy to result
- operator*(Series): result q_shift = a.q_shift + b.q_shift
- operator*(Frac): preserve q_shift
- inverse(): result q_shift = -a.q_shift (negate via Frac(0) - q_shift)
- pow(n): result q_shift = n * a.q_shift
- subs_q(k): result q_shift = k * a.q_shift
- truncTo(): copy q_shift
- str(): if q_shift != 0, prefix output with q^(shift) notation
- expToUnicode: add Frac overload for rational exponents
  </action>
  <done>Series has q_shift with full propagation</done>
</task>

<task type="auto">
  <name>Task 2: theta2 sets q_shift</name>
  <files>src/qfuncs.h</files>
  <action>
In theta2(), set result.q_shift = Frac(1, 4) before returning.
  </action>
  <done>theta2 carries q^(1/4) metadata</done>
</task>

<task type="auto">
  <name>Task 3: Update display for q_shift</name>
  <files>src/repl.h</files>
  <action>
In formatEtamake(), extract q_shift from the input series and include it in the
denominator display (matching Maple's convention). Also handle display in the
main series display path if q_shift is present.

Note: etamake operates on coefficients (which are correct), the q_shift just
needs to be passed through to the display.

Update etamake call in dispatchBuiltin to pass q_shift info through.
  </action>
  <done>Display includes q_shift in etamake output</done>
</task>

</tasks>

<verification>
1. theta2(100) displays with q^(1/4) prefix
2. etamake(theta2(100),100) includes q^(1/4) in output
3. Normal series arithmetic unchanged (q_shift=0 throughout)
4. theta2(100) * theta3(100) works (q_shifts add: 1/4 + 0 = 1/4)
5. theta2(100) + theta2(100) works (same q_shift)
6. All 10 acceptance tests pass
</verification>
