---
phase: 94-bq-block10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
  - src/series_omega.h
  - tests/maple-checklist.sh
  - tests/acceptance-exercises.sh
  - Makefile
autonomous: true

must_haves:
  truths:
    - "b(q) via eta identity etaq(1,T)^3/etaq(3,T) produces correct q-series (1 - 3q + ...)"
    - "b(q) via double-sum sum(sum(omega^(n-m)*q^(n^2+n*m+m^2), m, -N, N), n, -N, N) matches eta identity to truncation"
    - "a(q) = sum(sum(q^(n^2+n*m+m^2), m, -N, N), n, -N, N) computes correctly"
    - "c(q) = 3*etaq(3,T)^3/etaq(1,T) computes correctly"
    - "maple-checklist Block 10 no longer skips; omega and b(q) via eta identity work"
    - "a(q), b(q), c(q) from Exercise 4 are consistent (a not eta product; b, c are eta products)"
  artifacts:
    - path: src/repl.h
      provides: Sum handler SeriesOmega branch; toRationalSeries helper
      contains: "SeriesOmega"
    - path: src/series_omega.h
      provides: toRationalSeries when all coeffs rational
    - path: tests/acceptance-exercises.sh
      provides: b(q) double-sum vs eta match; a(q), c(q) consistency
    - path: tests/maple-checklist.sh
      provides: Block 10 real test replacing skip
  key_links:
    - from: src/repl.h
      to: src/series_omega.h
      via: "Sum case SeriesOmega accumulation"
    - from: tests/acceptance-exercises.sh
      to: b(q) eta vs double-sum
      via: "grep match for 1 - 3q or coefficient comparison"
    - from: tests/maple-checklist.sh
      to: Block 10
      via: "omega := RootOf(3); b := etaq(1,50)^3/etaq(3,50)"
---

<objective>
Implement b(q) computation via double-sum and eta identity, a(q) and c(q), and maple-checklist Block 10 parity. The eta identity b(q) = η(τ)³/η(3τ) already works (acceptance-exercises EX-04a/b). Add double-sum form for validation, Sum support for SeriesOmega, Block 10 real test, and Exercise 4 a/b/c consistency checks.

Purpose: Phase 94 success criteria ROOT-13, ROOT-14; complete Milestone v8.0 RootOf Function.

Output: Sum(SeriesOmega) works; b(q) double-sum matches eta identity; Block 10 passes; acceptance-exercises extended.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/93-parser-repl-omega/93-01-SUMMARY.md
@src/repl.h (Sum case, lines 2202–2226)
@src/series_omega.h
@qseriesdoc.md (Exercise 4)
@maple_checklist.md (Block 10)
@exercises_solutions.md (Exercise 4 a,b,c formulas)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sum handler SeriesOmega branch and toRationalSeries</name>
  <files>src/repl.h, src/series_omega.h</files>
  <action>
In repl.h case Expr::Tag::Sum (around line 2202): add a branch for SeriesOmega before the existing toSeries fallback. If std::holds_alternative&lt;SeriesOmega&gt;(first), accumulate SeriesOmega terms same as Omega3/Series: loop n from lo+1 to hi, eval body, check type, acc = acc + term, truncate.

In series_omega.h: add static Series toRationalSeries(const SeriesOmega&amp; s) — iterates coeffs, checks each Omega3 has .b == 0 (isRational), builds Series with Frac(a) at each exponent. If any coeff has b != 0, throw "SeriesOmega has non-rational coefficients". Used for b(q) verification.

Wire toRationalSeries in repl.h: add a path so that when comparing b(q) double-sum to eta identity, we can convert SeriesOmega to Series (or expose as optional builtin rationalPart(expr) if useful; per instructions optional builtins are acceptable).

Ensure BinOp omega^(n-m) * q^(n^2+n*m+m^2) produces SeriesOmega: omega^int gives Omega3, q^int gives Series, Omega3*Series = SeriesOmega. Parser already supports nested sum; body uses n,m from sumIndices.
</action>
  <verify>
Build: make clean; make. Run: echo "set_trunc(100)" "omega := RootOf(3)" "b_sum := sum(sum(omega^(n-m)*q^(n^2+n*m+m^2), m, -10, 10), n, -10, 10)" "b_sum" | ./qseries 2>&1 | grep -q "1 - 3q" or similar (b(q) starts 1 - 3q). Confirm no "mixed Omega3 and non-Omega3" or toSeries throw.
</verify>
  <done>sum(sum(omega^(n-m)*q^(n^2+n*m+m^2), m, -10, 10), n, -10, 10) evaluates to SeriesOmega; b(q) double-sum output starts "1 - 3q". toRationalSeries exists for verification.
</done>
</task>

<task type="auto">
  <name>Task 2: Block 10 real test and b(q) double-sum vs eta acceptance</name>
  <files>tests/maple-checklist.sh, tests/acceptance-exercises.sh</files>
  <action>
In maple-checklist.sh: replace the Block 10 skip (lines 92–93) with a real test. Run: omega := RootOf(3), then b := etaq(1,50)^3/etaq(3,50), then series(b, 20). Pass if output contains "1 - 3q" (or equivalent). Optionally also run etamake(b, 50) and grep for eta symbol. Per maple_checklist.md Block 10 only defines omega; the b(q) via eta identity satisfies Block 10 parity.

In acceptance-exercises.sh: add EX-04c: b(q) double-sum matches eta identity. Compute b_eta := etaq(1,50)^3/etaq(3,50) and b_sum := sum(sum(omega^(n-m)*q^(n^2+n*m+m^2), m, -12, 12), n, -12, 12) with set_trunc(60), omega from env (or omega := RootOf(3)). Use toRationalSeries or equivalent to compare: extract first few coefficients (e.g. series(b_sum, 10) vs series(b_eta, 10)) — if both display "1 - 3q" plus same next terms, pass. If toRationalSeries is internal-only, compare via string grep of distinctive substrings.

Add EX-04d: a(q) = sum(sum(q^(n^2+n*m+m^2), m, -15, 15), n, -15, 15), series starts "1 + 6q" per exercises_solutions. EX-04e: c(q) = 3*etaq(3,50)^3/etaq(1,50), etamake identifies eta quotient. Ensure Makefile runs acceptance-exercises as part of regression.
</action>
  <verify>
bash tests/maple-checklist.sh: Block 10 line shows pass (not skip). bash tests/acceptance-exercises.sh: EX-04a through EX-04e pass. Full regression: make acceptance-exercises (or equivalent target) passes.
</verify>
  <done>Block 10 passes; EX-04c (double-sum match), EX-04d (a(q)), EX-04e (c(q)) in acceptance-exercises; maple-checklist Block 10 no longer skips.
</done>
</task>

<task type="auto">
  <name>Task 3: Optional bq/aq/cq builtins and help</name>
  <files>src/repl.h</files>
  <action>
Optional per instructions: add bq(T), aq(T), cq(T) builtins if useful to reduce REPL verbosity. bq(T) := etaq(1,T)^3/etaq(3,T). aq(T) := sum(sum(q^(n^2+n*m+m^2), m, -N, N), n, -N, N) with N = max(15, ceil(sqrt(T/3)) + 2) or fixed N=20 for T up to ~200. cq(T) := 3*etaq(3,T)^3/etaq(1,T). Add to getHelpTable. If time-boxed, this task can be minimal (e.g. bq(T) only) or deferred — executor discretion. Primary success is Tasks 1–2.
</action>
  <verify>
If implemented: help(bq) shows signature; bq(50) returns same as etaq(1,50)^3/etaq(3,50). If skipped: document in SUMMARY; no regression.
</verify>
  <done>Either bq(T) (and optionally aq, cq) builtins with help, or explicit "deferred" note in SUMMARY. Phase 94 success criteria do not require builtins.
</done>
</task>

</tasks>

<verification>
- b(q) via eta identity: existing EX-04a/EX-04b pass
- b(q) double-sum: sum(sum(omega^(n-m)*q^(n^2+n*m+m^2), m, -N, N), n, -N, N) evaluates; matches eta identity to truncation (EX-04c)
- a(q), c(q): EX-04d, EX-04e pass
- maple-checklist Block 10: pass (not skip)
- Full regression: acceptance-exercises, acceptance-omega, maple-checklist pass
</verification>

<success_criteria>
1. b(q) via sum(omega^(n-m)*q^(n²+nm+m²)) or eta identity produces correct q-series
2. b(q) double-sum matches eta identity to truncation
3. a(q), b(q), c(q) from Exercise 4 computable and consistent
4. maple-checklist Block 10 no longer skips
5. Acceptance test: b(q) coefficients for small exponents match known values (1 - 3q + ...)
</success_criteria>

<output>
After completion, create .planning/phases/94-bq-block10/94-01-SUMMARY.md
</output>
