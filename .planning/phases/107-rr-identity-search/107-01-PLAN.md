---
phase: 107-rr-identity-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rr_ids.h
  - src/repl.h
  - tests/acceptance-rr-id.sh
autonomous: true

must_haves:
  truths:
    - "findids(4, T) runs and returns 3-tuples [p,a,c1] or empty list"
    - "findids(5, T) runs and returns 3-tuples [a,p,c1] or empty list"
    - "At least one of findids(4) or findids(5) discovers ≥1 identity (type 5 (1,1) expected)"
    - "acceptance-rr-id.sh passes including findids(4) and findids(5) tests"
  artifacts:
    - path: src/rr_ids.h
      provides: findids_type4, findids_type5, findids dispatch
      contains: "findids_type4", "findids_type5", "case 4", "case 5"
    - path: tests/acceptance-rr-id.sh
      provides: findids(4) and findids(5) acceptance tests
      contains: "findids(4", "findids(5"
  key_links:
    - from: src/rr_ids.h findids()
      to: findids_type4, findids_type5
      via: switch/case dispatch
      pattern: "case 4|case 5"
    - from: findids_type4
      to: RRGstar, RRHstar, addSeriesAligned, checkid
      via: SYMF GM(p)HM(a)±GM(a)HM(p)
    - from: findids_type5
      to: RRGstar, RRHstar, addSeriesAligned, checkid
      via: SYMF GM(a)GM(p)±HM(a)HM(p)
---

<objective>

Extend findids to types 4 and 5 (RRGstar/RRHstar SUM forms); extend acceptance-rr-id.sh to verify both. Delivers "at least 2 of types 3–10" via types 4 and 5.

Purpose: Satisfy GAP11-06, GAP11-07; findids discovers identities for types 4 and 5.
Output: findids_type4, findids_type5 in rr_ids.h; findids(4), findids(5) in REPL; acceptance-rr-id.sh tests 10–11.

</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-rr-identity-search/107-RESEARCH.md
@src/rr_ids.h
@tests/acceptance-rr-id.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement findids_type4 and findids_type5</name>
  <files>src/rr_ids.h</files>
  <action>

Implement findids_type4 and findids_type5 in rr_ids.h per 107-RESEARCH.md. Use FindidTuple (3-tuple); RE uses RRGstar/RRHstar; loop structure mirrors findids_type1/type2.

**findids_type4** — SYMF: GM(p)HM(a) ± GM(a)HM(p)
- Loop: n=2..T, p ∈ divisors(n), a = n/p
- Constraints: a≠p, a≤p, gcd(a,p)=1, (a mod 2 = 0) ∨ (p mod 2 = 0)
- ABCOND: abcond_type1(p, a)
- Code: t1 = RRGstar(p,T)*RRHstar(a,T); t2 = RRGstar(a,T)*RRHstar(p,T); symf = addSeriesAligned(t1, t2, c1, T)
- Return: (p, a, c1)

**findids_type5** — SYMF: GM(a)GM(p) ± HM(a)HM(p)
- Loop: n=1..T, p ∈ divisors(n), a = n/p
- Constraints: a≤p, gcd(a,p)=1, (a mod 2 = 0) ∨ (p mod 2 = 0)
- ABCOND: abcond_type2(a, p)
- Code: t1 = RRGstar(a,T)*RRGstar(p,T); t2 = RRHstar(a,T)*RRHstar(p,T); symf = addSeriesAligned(t1, t2, c1, T)
- Return: (a, p, c1)

Extend findids(type, T, acc) to dispatch types 4 and 5 (switch or if-chain). Types 4–5 return same FindidTuple as types 1–2; REPL needs no change.

Note: RRGstar(n)/RRHstar(n) for n>1 are stubbed to RRGstar1/RRHstar1. Type 5 (a,p)=(1,1) is fully supported; type 4 (2,1) may still run but discovery depends on stub correctness.

</action>
  <verify>Build: g++ -std=c++20 -O2 -o qseries main.cpp 2>&1; run: printf 'set_trunc(50)\nfindids(4,20)\nfindids(5,20)\n' | ./qseries.exe 2>/dev/null — both return 0 and print lines [p,a,c1] or [a,p,c1]; findids(5,20) expected to show at least [1,1,-1] or [1,1,1]</verify>
  <done>findids(4,T) and findids(5,T) run and return 3-tuples; type 5 discovers at least one identity</done>
</task>

<task type="auto">
  <name>Task 2: Extend acceptance-rr-id.sh and help text</name>
  <files>tests/acceptance-rr-id.sh, src/repl.h</files>
  <action>

**acceptance-rr-id.sh:** Add tests 10 and 11 after the existing findids(2, 20) test (around line 115):

- Test 10: findids(4, 20) — run and verify output format (lines like [p,a,c1]); count identities; PASS if completes
- Test 11: findids(5, 20) — same; require at least one identity found (count2 ≥ 1 for type 5, or combined count for types 4+5 ≥ 1)

Pattern from existing tests 8–9: run findids, grep for tuple lines, count. Require: findids(4) and findids(5) both complete; at least one discovers ≥1 identity.

**repl.h:** Update findids help string (around line 477) to mention types 4 and 5:
- e.g. "search RR identities: type 1 (G(p)H(a)±...), type 2 (G(a)G(p)±...), type 4 (GM(p)HM(a)±...), type 5 (GM(a)GM(p)±...)"

</action>
  <verify>bash tests/acceptance-rr-id.sh 2>&1; exit 0 with "All Phase 86 (RR Identity Search) tests PASS"</verify>
  <done>acceptance-rr-id.sh passes; findids(4) and findids(5) tested; help text updated</done>
</task>

</tasks>

<verification>

1. g++ -std=c++20 -O2 -o qseries main.cpp
2. printf 'set_trunc(50)\nfindids(4,20)\nfindids(5,20)\n' | ./qseries.exe — both produce output
3. bash tests/acceptance-rr-id.sh — passes

</verification>

<success_criteria>

- findids(4, T) and findids(5, T) work and return 3-tuples
- At least one of types 4 or 5 discovers ≥1 identity (type 5 (1,1) expected)
- acceptance-rr-id.sh passes
- GAP11-06, GAP11-07 satisfied for types 4–5 scope

</success_criteria>

<output>

After completion, create `.planning/phases/107-rr-identity-search/107-01-SUMMARY.md`

</output>
