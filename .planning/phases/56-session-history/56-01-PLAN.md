---
phase: 56-session-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
autonomous: true

must_haves:
  truths:
    - "Command history saved to ~/.qseries_history on exit, loaded on startup"
    - "Max 1000 lines in history file"
    - "save(name) serializes environment to .qsession file"
    - "load(name) restores variables from .qsession file"
    - "History persistence works on Unix and Windows"
    - "All existing acceptance tests pass"
  artifacts:
    - path: "src/repl.h"
      provides: "History persistence, save/load commands"
      contains: "qseries_history"
---

<objective>
Add history persistence and session save/load to the REPL:
1. History: save command history to ~/.qseries_history on exit, load on startup (max 1000 lines)
2. Save/load: save(name) serializes env to .qsession file, load(name) restores it

Purpose: Persist state across sessions.
Output: Modified `src/repl.h`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: History persistence</name>
  <files>src/repl.h</files>
  <action>
Add history file load/save to runRepl(). Add fstream include.

1. Add getHomeDir() helper for cross-platform home directory
2. Add loadHistory() and saveHistory() functions
3. Call loadHistory at REPL start, saveHistory at REPL end
4. Increase maxHistory from 100 to 1000
  </action>
  <done>History persistence implemented</done>
</task>

<task type="auto">
  <name>Task 2: Save/load session commands</name>
  <files>src/repl.h</files>
  <action>
Add save(name) and load(name) built-in commands in dispatchBuiltin.

1. save(name) writes env vars as "varname := series_string" lines to name.qsession
2. load(name) reads .qsession file, parses and evaluates each line
3. Add help table entries
  </action>
  <done>Save/load session commands implemented</done>
</task>

</tasks>

<verification>
1. History persists across sessions
2. save/load round-trips variables correctly
3. All acceptance tests pass
4. Works on Unix and Windows
</verification>
