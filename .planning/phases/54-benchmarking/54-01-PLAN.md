---
phase: 54-benchmarking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bench_main.cpp
  - Makefile
autonomous: true

must_haves:
  truths:
    - "bench_main.cpp exists as a separate binary with micro-benchmarks"
    - "Benchmarks cover BigInt multiply, Series multiply, etaq, and prodmake"
    - "Results report median of multiple runs"
    - "make bench builds and runs the benchmark suite"
  artifacts:
    - path: "src/bench_main.cpp"
      provides: "Benchmarking suite"
      contains: "benchmark"
    - path: "Makefile"
      provides: "bench target"
      contains: "bench"
---

<objective>
Create a standalone benchmarking binary (bench_main.cpp) with micro-benchmarks for the four core operations: BigInt multiply, Series multiply, etaq, and prodmake. Use std::chrono::steady_clock for timing, DoNotOptimize barriers to prevent dead-code elimination, and median reporting for noise resistance. Add `make bench` target.

Purpose: Developers can measure and compare performance of core operations.
Output: New `src/bench_main.cpp`, updated `Makefile`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create bench_main.cpp and Makefile target</name>
  <files>src/bench_main.cpp, Makefile</files>
  <action>
Create src/bench_main.cpp and add make bench target.
  </action>
  <verify>
1. make bench builds and runs
2. Output shows median timings for 4 benchmarks
3. Builds without warnings
  </verify>
  <done>Benchmarking suite created</done>
</task>

</tasks>
