---
phase: 39-update-manual
plan: 01
status: COMPLETED
estimated_duration: ~10min
---

## Goal

Update MANUAL.md with all 10 new v1.8 built-in functions and fix stale version references.

## Requirements Traced

- DOC-03: MANUAL.md updated with all 10 new v1.8 built-ins
- DOC-04: MANUAL.md version references updated to 1.9

## Analysis

### Current state of MANUAL.md

The manual was written at v1.7 with 37 built-ins documented. It's missing all 10 functions added in v1.8 (phases 36-38). The `version()` example still shows `qseries 1.3`.

### Functions to add, by section

**Series Inspection** (after `sift`):
- `coeff(f, n)` — coefficient of q^n in series f
- `dissect(f, m, T)` — all m siftings of f
- `qdiff(f)` — q-derivative

**Series Construction** (after `sum`/`add`):
- `eisenstein(k, T)` — normalized Eisenstein series E_{2k}(q)

**Number Theory Helpers** (after `legendre`):
- `divisors(n)` — sorted list of positive divisors
- `euler_phi(n)` — Euler's totient
- `jacobi(a, n)` — Jacobi symbol
- `kronecker(a, n)` — Kronecker symbol
- `mobius(n)` — Möbius function
- `partition(n)` — partition number p(n)

### Version references to fix

- Line 483: `qseries 1.3` → `qseries 1.9`

## Tasks

### Task 1: Add to Series Inspection section

After the `sift` entry (line ~301), add:

- `coeff(f, n)` with example: `coeff(etaq(1, 30), 5)` → `1`
- `dissect(f, m, T)` with example: `dissect(etaq(1,30), 3, 10)`
- `qdiff(f)` with example: `qdiff(1 + q + q^2 + q^3)` → `q + 2q² + 3q³`

### Task 2: Add eisenstein to Series Construction section

After `sum`/`add` (line ~245), add:

- `eisenstein(k, T)` with example: `eisenstein(2, 10)` → `1 + 240q + 2160q² + ...`

### Task 3: Expand Number Theory Helpers section

After `legendre` (line ~449), add entries for:

- `divisors(n)` with example: `divisors(12)` → `[1, 2, 3, 4, 6, 12]`
- `euler_phi(n)` with example: `euler_phi(12)` → `4`
- `jacobi(a, n)` with example: `jacobi(2, 15)` → `1`
- `kronecker(a, n)` with example: `kronecker(-1, -1)` → `-1`
- `mobius(n)` with example: `mobius(30)` → `-1`
- `partition(n)` with example: `partition(100)` → `190569292`

### Task 4: Fix version references

- Update `version()` example output from `qseries 1.3` to `qseries 1.9`
- Update version string in `src/repl.h` banner and version() output to `1.9`

### Task 5: Build and verify

- Build to ensure version change compiles
- Run acceptance tests to confirm nothing broken

## Verification Criteria

- [ ] All 10 functions documented with signature, description, and example
- [ ] Version references show 1.9
- [ ] Manual sections remain well-organized and alphabetical within sections
- [ ] Build succeeds, all tests pass
