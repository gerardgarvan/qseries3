---
phase: 50-example-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/garvan-demo.sh
  - MANUAL.md
  - index.html
autonomous: true

must_haves:
  truths:
    - "Every command in demo/garvan-demo.sh runs without error and produces correct output"
    - "Every example in MANUAL.md runs without error and produces correct output"
    - "Every code snippet in index.html runs without error and produces correct output"
    - "Any failing examples have been fixed in the example text or in the REPL code"
  artifacts:
    - path: "demo/garvan-demo.sh"
      provides: "Audited demo script"
      contains: "prodmake"
    - path: "MANUAL.md"
      provides: "Audited manual"
      contains: "qseries>"
    - path: "index.html"
      provides: "Audited website"
      contains: "prodmake"
---

<objective>
Audit every example across all project documentation. Run each command through the qseries binary, compare output to what's documented, and fix any discrepancies. The three sources are: demo/garvan-demo.sh, MANUAL.md, and index.html.

Purpose: Ensure all documented examples actually work and produce the output shown.
Output: Fixed files where needed, audit report in SUMMARY.md.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit demo/garvan-demo.sh</name>
  <files>demo/garvan-demo.sh</files>
  <action>
Run the full demo script through the qseries binary and check for errors.

**Method:**
1. Read `demo/garvan-demo.sh` to understand all commands
2. Extract each `run` block's commands
3. Pipe each command set to `./qseries.exe` and check:
   - No error messages in output
   - Output is mathematically reasonable (prodmake shows product form, etamake shows eta products, etc.)
4. If any command fails or produces an error, diagnose and fix:
   - If it's a script bug (wrong syntax, missing semicolons): fix the script
   - If it's a REPL bug: note it but don't fix C++ code in this phase

**Expected sections in garvan-demo.sh:**
- Rogers-Ramanujan (sum + series + prodmake)
- qfactor T(8,8)
- etamake on theta2, theta3, theta4
- jacprodmake on Rogers-Ramanujan
- findhom Gauss AGM
- findhomcombo
- findnonhomcombo Watson
- Sifting Rødseth
- Triple product
- Quintuple product + Euler dissection
- Winquist

Run the entire script via Cygwin bash:
```bash
C:\cygwin64\bin\bash.exe -lc "cd /home/ggarv/qseries3 && bash demo/garvan-demo.sh"
```

Record which sections pass/fail.
  </action>
  <verify>
1. Full demo script runs to completion without bash errors
2. No "error:" lines in qseries output (except intentional ones)
3. Key outputs verified: prodmake shows product, etamake shows eta, findhom shows relations
  </verify>
  <done>demo/garvan-demo.sh audit complete — all sections pass or fixes applied</done>
</task>

<task type="auto">
  <name>Task 2: Audit MANUAL.md examples</name>
  <files>MANUAL.md</files>
  <action>
Extract every `qseries>` example from MANUAL.md and run through the binary.

**Method:**
1. Read MANUAL.md
2. Find all lines starting with `qseries>` (these are example inputs)
3. For each example, pipe the command to `./qseries.exe`
4. Compare the actual output to what's shown in the manual
5. If output differs: update the manual to show the correct output
6. If the command errors: check if syntax is wrong and fix, or note as a REPL issue

**Categories to check:**
- Getting started examples
- Each built-in function example (all 50+)
- Workflow tutorial examples (Rogers-Ramanujan, product identification, relations)
- REPL feature examples (semicolons, backslash continuation, etc.)

Focus on commands that produce series output — these are most likely to have been manually written and potentially incorrect.
  </action>
  <verify>
1. All extracted examples run without error
2. Outputs in MANUAL.md match actual REPL output
3. Any fixes preserve the manual's teaching narrative
  </verify>
  <done>MANUAL.md examples audit complete — all verified or fixed</done>
</task>

<task type="auto">
  <name>Task 3: Audit index.html examples</name>
  <files>index.html</files>
  <action>
Extract every REPL example from the website and verify.

**Method:**
1. Read index.html
2. Find all `<span class="i">` (input) and `<span class="o">` (output) pairs
3. Run each input through `./qseries.exe`
4. Compare to the displayed output
5. Fix any mismatches

**Known examples in index.html:**
- Rogers-Ramanujan: sum + prodmake
- Partition function: 1/etaq(1,30) + partition(100)
- Theta → Eta: etamake(theta3(50), 50)
- Relations: eisenstein + findpoly

Also check the demo script section — verify the script text is valid bash that would run correctly.
  </action>
  <verify>
1. All 4 website examples produce the output shown
2. Demo script section text is syntactically valid
3. No HTML encoding issues affecting copy-paste (& vs &amp;, etc.)
  </verify>
  <done>index.html examples audit complete — all verified or fixed</done>
</task>

</tasks>

<verification>
1. `demo/garvan-demo.sh` runs end-to-end without errors
2. All MANUAL.md `qseries>` examples produce correct output
3. All index.html examples produce the output shown
4. Any fixes committed with clear descriptions
</verification>

<success_criteria>
- Zero failing examples across all three documentation sources
- All outputs match what's documented
- Fixes applied to docs (not C++ code) unless there's a genuine REPL bug
</success_criteria>

<output>
After completion, create `.planning/phases/50-example-audit/50-01-SUMMARY.md`
</output>
