---
phase: 93-parser-repl-omega
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
  - src/series_omega.h
  - tests/acceptance-omega.sh
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Typing `omega` in REPL returns Omega3(0,1) and displays as omega"
    - "RootOf(3) and RootOf([1,1,1]) both return Omega3(0,1)"
    - "sum(omega^n, n, 0, 2) evaluates to 1 + omega + omega^2 = 0"
    - "omega := RootOf(3) stores; subsequent `omega` or `omega*2` uses stored value"
    - "omega^2 displays correctly when evaluated"
  artifacts:
    - path: src/repl.h
      provides: EnvValue/EvalResult with Omega3, Var lookup, RootOf dispatch, BinOp omega ops, Sum Omega3, Assign, display
    - path: src/series_omega.h
      provides: SeriesOmega::str(maxTerms) for display
    - path: tests/acceptance-omega.sh
      provides: Phase 93 acceptance tests
  key_links:
    - from: evalExpr Tag::Var
      to: env.env["omega"]
      via: pre-populated or lookup returns Omega3
      pattern: "omega.*Omega3::omega"
    - from: dispatchBuiltin RootOf
      to: Omega3::omega()
      via: RootOf(3) or RootOf(1 int arg)
      pattern: "RootOf"
    - from: evalExpr Tag::Sum
      to: Omega3 accumulation
      via: body evaluates to Omega3 when omega^n with n from sum index
      pattern: "std::holds_alternative<Omega3>"
---

<objective>
Integrate omega (Omega3) and RootOf into the parser/REPL so the 5 Phase 93 success criteria are satisfied.

Purpose: Enable omega symbol, RootOf(3) dispatch, sum(omega^expr), and variable assignment for omega (ROOT-09..12).

Output: repl.h extended; series_omega.h gains str(); acceptance-omega.sh tests all criteria.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-series-omega/92-01-SUMMARY.md
@src/omega3.h
@src/series_omega.h
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: EnvValue, EvalResult, includes, env pre-populate, Var lookup, RootOf, Assign, display</name>
  <files>src/repl.h, src/series_omega.h</files>
  <action>
Add omega/Omega3/SeriesOmega integration to REPL.

1. Includes: Add `#include "omega3.h"` and `#include "series_omega.h"` after series.h.

2. EnvValue: Add Omega3 to the variant (line ~182):
   `using EnvValue = std::variant<Series, std::vector<JacFactor>, Partition, Phi1Result, RelationKernelResult, Omega3>;`

3. EvalResult: Add Omega3 and SeriesOmega to the variant (line ~368):
   Insert Omega3 and SeriesOmega before Phi1Result.

4. Environment(): In constructor, after `env["q"] = Series::q(T);` add:
   `env["omega"] = Omega3::omega();`

5. getSeriesFromEnv: Add branch for Omega3 that throws "variable holds Omega3, not series".

6. evalExpr Tag::Var: After sumIndices check, if ev == env.env.end() and e->varName == "omega", return Omega3::omega() (built-in fallback — optional if pre-populated). When ev != end and std::holds_alternative&lt;Omega3&gt;(ev->second), return std::get&lt;Omega3&gt;(ev->second) instead of getSeriesFromEnv.

7. dispatchBuiltin: Add RootOf handler. Support two forms: (a) RootOf(3) — one int arg → Omega3::omega(); (b) RootOf([1,1,1]) — one List arg of 3 integers [1,1,1] (coefficients of z²+z+1) → Omega3::omega(). This covers both success-criterion forms: RootOf(3) and the polynomial form (Maple's RootOf(z²+z+1=0) maps to RootOf([1,1,1]) for v1). If args.size() != 1, throw. For List arg: extract 3 elements via evalToInt, require [1,1,1] or equivalent; else throw.

8. evalStmt Assign: Add branch for Omega3: if std::holds_alternative&lt;Omega3&gt;(res), store in env.env[s->assignName] and return.

9. display: Add branches for Omega3 and SeriesOmega. Omega3: std::cout &lt;&lt; arg.str() &lt;&lt; std::endl. SeriesOmega: std::cout &lt;&lt; arg.str(30) &lt;&lt; std::endl (requires SeriesOmega::str).

10. series_omega.h: Add str(int maxTerms = 30) to SeriesOmega, similar to Series::str: iterate c in order, format each term as coeff.str() + (exp==0 ? "" : "*q^" + expToUnicode(exp)), join with " + ", append " + O(q^T)" or similar. Use Series::expToUnicode from series.h for exponent formatting.
  </action>
  <verify>Build succeeds. Run `echo "omega" | ./qseries` and confirm output is "omega". Run `echo "RootOf(3)" | ./qseries` and confirm "omega".</verify>
  <done>omega and RootOf(3) evaluate and display; omega := RootOf(3) stores; omega*2 displays correctly.</done>
</task>

<task type="auto">
  <name>Task 2: BinOp omega^int, Omega3 arithmetic, isOmegaLike</name>
  <files>src/repl.h</files>
  <action>
Add BinOp handling for omega^exponent, Omega3+Omega3, Omega3*scalar, Omega3*Series.

1. isOmegaLike: Add helper (near isQLike, ~line 601). Return true if expr is Var "omega" OR evaluates to Omega3. For simplicity: check `e && e->tag == Expr::Tag::Var && e->varName == "omega"` — that covers the common case. A more complete version would eval left and check for Omega3, but Var "omega" suffices for sum(omega^n) and omega^2.

2. evalExpr Tag::BinOp: Before the existing isQLike block (q^exp), add handling for omega^exponent:
   - If e->binOp == Pow and isOmegaLike(e->left.get()):
     - Evaluate right with evalToInt (for sum index n or literal). If successful, k = evalToInt result; return Omega3::pow(Omega3::omega(), static_cast&lt;int&gt;(k)).
     - If left evaluates to Omega3 (from env lookup) and right is integer, use that Omega3 for pow base. For Var "omega" we use Omega3::omega() as base. If env has overridden omega, get it: eval left, if Omega3 use it.
   - Implement: EvalResult leftRes = eval(e->left.get(), env, sumIndices). If std::holds_alternative&lt;Omega3&gt;(leftRes), try int k = evalToInt(e->right...). Return Omega3::pow(std::get&lt;Omega3&gt;(leftRes), k).

3. BinOp Add: Before converting both sides to Series, check if either evaluates to Omega3. If both are Omega3: eval left and right, if both Omega3 return left+right. If one Omega3 and one Series, convert Omega3 to SeriesOmega::constant(omega,T) and add? No — Omega3 + Series is not defined in Phase 92. For sum(omega^n) we only need Omega3 + Omega3. So: if leftRes and rightRes are both Omega3, return left+right. If one is Omega3 and one is Series, throw "omega + series not supported" or defer. Success criteria only require omega + omega + omega^2 = 0.

4. BinOp Mul: If left is Omega3 and right is Series → return Omega3 * Series (operator in series_omega.h). If left is Series and right is Omega3 → return Series * Omega3 (need Series * Omega3 — check series_omega: we have Omega3 * Series. Add Series * Omega3 if needed, or handle only omega*Series for now). If both Omega3 → return left * right. If Omega3 and int64_t (from eval): Omega3 * Omega3::fromRational(Frac(n)).

5. Order of BinOp dispatch: First try omega^int (Pow + isOmegaLike). Then try omega*Series, omega+omega, etc. The existing path converts to Series via toSeries — we must intercept before that for Omega3.

6. Refactor BinOp flow: Evaluate left and right first. If (leftRes is Omega3 or rightRes is Omega3), handle Omega3 cases: Pow with left=Omega3 and right=int; Add with both Omega3; Mul with Omega3*Series, Series*Omega3, Omega3*Omega3, Omega3*int. For Mul with int: if rightRes is int64_t, treat as Omega3::fromRational. If leftRes is int64_t and rightRes is Omega3, same.

7. toSeries: When called on Omega3, throw "expected series" (no automatic conversion of scalar omega to series).
  </action>
  <verify>echo "omega^2" | ./qseries prints "omega^2". echo "omega*2" | ./qseries prints "2*omega". echo "omega*(1+q)" | ./qseries prints SeriesOmega form.</verify>
  <done>omega^2 and omega*scalar and omega*Series work.</done>
</task>

<task type="auto">
  <name>Task 3: Sum with Omega3 body, acceptance tests</name>
  <files>src/repl.h, tests/acceptance-omega.sh</files>
  <action>
1. Sum: Modify the Sum case in evalExpr. Currently it accumulates Series via toSeries(body). For sum(omega^n, n, 0, 2), the body omega^n evaluates to Omega3. So:
   - Evaluate body once for first iteration. If result is Omega3, use Omega3 accumulation: Omega3 acc = Omega3(0,0); for n=lo..hi { idx[var]=n; EvalResult term = eval(body, env, idx); if (std::holds_alternative&lt;Omega3&gt;(term)) acc = acc + std::get&lt;Omega3&gt;(term); else if (std::holds_alternative&lt;SeriesOmega&gt;(term)) ... } return acc;
   - Actually: sum(omega^n, n, 0, 2) gives terms 1, omega, omega^2 — all Omega3. So we only need Omega3 accumulation. If the first term is Omega3, accumulate Omega3; return Omega3 at end.
   - Implementation: EvalResult first = eval(body, env, idx with n=lo). If holds_alternative&lt;Omega3&gt;(first), then Omega3 acc = std::get&lt;Omega3&gt;(first); for n=lo+1 to hi { idx[var]=n; term = eval(body...); if holds Omega3, acc += get; else throw } return acc;
   - If first term is Series or SeriesOmega, keep existing Series accumulation. For SeriesOmega we'd need SeriesOmega acc and +=. Add that branch: if first is SeriesOmega, accumulate SeriesOmega.
   - For mixed (Omega3 + SeriesOmega) — not in success criteria; throw if mixed.

2. getSeriesFromEnv: When EnvValue is Omega3, we already throw. Good.

3. Create tests/acceptance-omega.sh:
   - BIN=./qseries or dist/qseries.exe if present
   - Test 1: omega -> "omega"
   - Test 2: RootOf(3) -> "omega"; RootOf([1,1,1]) -> "omega"
   - Test 3: sum(omega^n, n, 0, 2) -> "0"
   - Test 4: omega := RootOf(3); omega -> "omega"
   - Test 5: omega^2 -> "omega^2"
   Use grep or pattern match on output. Example: `echo "omega" | $BIN 2>/dev/null | grep -q "omega"`.
   Add to Makefile: acceptance-omega target that runs the script.
  </action>
  <verify>make acceptance-omega or bash tests/acceptance-omega.sh passes all 5 tests.</verify>
  <done>sum(omega^n, n, 0, 2) = 0; all 5 Phase 93 success criteria verified by acceptance-omega.sh.</done>
</task>

</tasks>

<verification>
- Build: make (or g++ -std=c++20 -O2 -o qseries main.cpp) succeeds.
- All 5 Phase 93 success criteria hold: omega, RootOf(3), sum(omega^n,n,0,2)=0, omega:= assignment, omega^2 display.
- tests/acceptance-omega.sh passes.
- No regressions: run a quick smoke test (e.g., etaq(1,10) or prodmake on rr) to ensure existing behavior unchanged.
</verification>

<success_criteria>
1. Typing `omega` returns Omega3(0,1) and displays "omega"
2. RootOf(3) and RootOf([1,1,1]) both return Omega3(0,1)
3. sum(omega^n, n, 0, 2) evaluates to 0
4. omega := RootOf(3) stores; subsequent omega or omega*2 uses stored value
5. omega^2 displays "omega^2"
</success_criteria>

<output>
After completion, create `.planning/phases/93-parser-repl-omega/93-01-SUMMARY.md`
</output>
