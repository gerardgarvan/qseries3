---
phase: 62-maple-checklist
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - tests/maple-checklist.sh
  - maple_checklist.md
  - exercises_solutions.md
autonomous: true

must_haves:
  truths:
    - "Blocks 28-41 have been attempted and status recorded in maple_checklist.md"
    - "Symbolic z blocks (28, 30, 31, 32) are documented as known limitations"
    - "tripleprod/quinprod concrete blocks (29, 36) produce correct output with fixed T semantics"
    - "Winquist blocks (37-41) produce correct results with manually expanded Q(k) definitions"
    - "All 13 exercises have qseries3 commands and output in exercises_solutions.md"
    - "Final maple_checklist.md reflects complete pass/fail status for all 41 blocks"
  artifacts:
    - path: "tests/maple-checklist.sh"
      provides: "Complete test script covering all 41 blocks"
      contains: "Block 41"
    - path: "maple_checklist.md"
      provides: "Complete checklist with all 41 blocks marked"
      contains: "[x] Verified"
    - path: "exercises_solutions.md"
      provides: "qseries3 commands and output for all 13 exercises"
      contains: "Exercise 13"
  key_links:
    - from: "tests/maple-checklist.sh"
      to: "maple_checklist.md"
      via: "test results inform checklist marks"
      pattern: "Block (2[89]|3[0-9]|4[01])"
---

<objective>
Complete Maple checklist blocks 28-41, write all 13 exercise solutions, and produce final artifacts.

Purpose: Blocks 28-41 cover triple/quintuple products, Winquist's identity, and sifting — areas requiring the fixed tripleprod/quinprod from Plan 01. Exercise solutions demonstrate qseries3's coverage of the Garvan tutorial. This plan produces the final deliverables.

Output: Complete test script (all 41 blocks), fully-updated maple_checklist.md, exercises_solutions.md.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-maple-checklist/62-01-SUMMARY.md
@.planning/phases/62-maple-checklist/62-RESEARCH.md
@.planning/phases/62-maple-checklist/62-CONTEXT.md

@src/qfuncs.h
@maple_checklist.md
@tests/maple-checklist.sh
@qseriesdoc.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add blocks 28-41 to test script and update checklist</name>
  <files>tests/maple-checklist.sh, maple_checklist.md</files>
  <action>
Extend `tests/maple-checklist.sh` with blocks 28-41. Update `maple_checklist.md` with results.

**Block 28** (symbolic z): Skip with note. `tripleprod(z,q,10)` requires symbolic variable z which qseries3 doesn't support. Print "SKIP: Block 28 (symbolic z)".

**Block 29**: `tripleprod(q, q^3, 100)`. After Plan 01's fix, this should produce the Euler pentagonal number series: `1 - q - q² + q⁵ + q⁷ - q¹² - q¹⁵ + q²² + q²⁶ - q³⁵ - q⁴⁰ + q⁵¹ + q⁵⁷`. Check for "q⁵⁷" or the coefficient at exponent 57.

**Block 30** (quinprod prodid): Skip. `quinprod(z,q,prodid)` requires symbolic mode. Print "SKIP: Block 30 (symbolic prodid mode)".

**Block 31** (quinprod seriesid): Skip. Same reason. Print "SKIP: Block 31 (symbolic seriesid mode)".

**Block 32** (symbolic z): Skip. `quinprod(z,q,3)` needs symbolic z. Print "SKIP: Block 32 (symbolic z)".

**Block 33**: `EULER := etaq(1, 500)` then `E0 := sift(EULER, 5, 0, 499)`. Check for non-zero series output.

**Block 34**: `jp := jacprodmake(E0, 50)`. Check for JAC output.

**Block 35**: `jac2prod(jp)`. Check for product notation.

**Block 36**: `qp := quinprod(q, q^5, 100)` then `series(qp, 100)`. After the fix, should have terms to ~q^85+. Check for a known coefficient.

**Block 37** (Winquist Q(k) expansion): Manually expand `Q(n) = tripleprod(q^n, q^33, T)` for each needed value. Use large T (e.g., 200) for correct truncation.
```
A0 := tripleprod(q^15, q^33, 200)
A3 := tripleprod(q^12, q^33, 200)
A7 := tripleprod(q^6, q^33, 200)
A8 := tripleprod(q^3, q^33, 200)
A9 := tripleprod(q^9, q^33, 200)
B2t := tripleprod(q^13, q^33, 200)
B2u := tripleprod(q^2, q^33, 200)
B2 := B2t - q^3 * B2u
B4t := tripleprod(q^7, q^33, 200)
B4u := tripleprod(q^4, q^33, 200)
B4 := B4t + q * B4u
IDG := series(A0*B2 - q^2*A9*B4, 200)
series(IDG, 10)
```
Check first few terms match expected: `1 - 2q - q² + 2q³ + q⁴ + 2q⁵ - 2q⁶ - 2q⁸ - 2q⁹ + ...` (coefficients from Maple output at line 1264 of qseriesdoc.md).

**Block 38**: `jp := jacprodmake(IDG, 50)`. Check for JAC output.

**Block 39**: `jac2prod(jp)`. Check for product notation.

**Block 40**: `series(winquist(q^5, q^3, q^11, 200), 20)`. Use T=200 for winquist to compute enough terms. Check first few coefficients.

**Block 41**: `series(IDG - winquist(q^5, q^3, q^11, 200), 60)`. Should be zero (O(q^60)). Check that output is `O(q^...)` or all-zero.

After running, update `maple_checklist.md`:
- Mark passing blocks with `[x]`
- Mark symbolic/infeasible blocks with `[~]` and explanatory note
- Mark failing blocks with `[ ]` and failure reason

Update the final summary line in the test script to show total PASS/FAIL/SKIP counts across all 41 blocks.
  </action>
  <verify>
1. `bash tests/maple-checklist.sh` runs all 41 blocks and reports complete pass/fail/skip.
2. Block 29 (tripleprod pentagonal) passes — terms up to q^57 present.
3. Block 41 (IDG - winquist) passes — difference is zero.
4. `maple_checklist.md` has every block marked.
  </verify>
  <done>All 41 blocks tested: 30+ pass, 5 skipped (symbolic/infeasible), remainder documented with failure reason. maple_checklist.md fully updated.</done>
</task>

<task type="auto">
  <name>Task 2: Create exercises_solutions.md with all 13 exercise solutions</name>
  <files>exercises_solutions.md</files>
  <action>
Create `exercises_solutions.md` in the repo root. For each of the 13 exercises, provide:
1. Exercise number and brief description
2. qseries3 commands (ready to paste into REPL)
3. Expected mathematical result (from qseriesdoc.md)
4. Actual qseries3 output (run the commands and capture output)

**Exercise 1** (Rogers-type series → product):
```
x := sum(q^(n^2)/aqprod(q,q,2*n,50), n, 0, 8)
series(x, 50)
prodmake(x, 40)
```
Expected: product with denominators at specific exponents.

**Exercise 2** (T(r,n) factorization):
```
T(4,4)
qfactor(T(4,4), 20)
T(6,6)
qfactor(T(6,6), 20)
T(10,10)
qfactor(T(10,10), 20)
```
Show pattern: T(r,n) factorizes into q-products.

**Exercise 3** (Dixson-like sum):
For general a, compute dixson(a,a,a,q) using the sum from Block 6 pattern. Try a=3,4,6:
```
d3 := sum((-1)^k * q^(k*(3*k+1)/2) * qbin(3+k,6,50)^3, k, -3, 3)
qfactor(d3, 20)
```

**Exercise 4** (a(q), b(q), c(q) eta products):
a(q) via double sum: `aq := sum(sum(q^(n^2+n*m+m^2), m, -20, 20), n, -20, 20)` — truncate to avoid large exponents. Then `etamake(aq, 100)`.

c(q) via double sum with shifted indices: `cq := sum(sum(q^((3*n+1)^2/3 + (3*n+1)*(3*m+1)/3 + (3*m+1)^2/3), m, -10, 10), n, -10, 10)`. Note: the exponent simplifies to `(n^2+n*m+m^2) + (n+m) + 1/3` when substituting n→n+1/3, m→m+1/3. So `cq := q^(1/3) * sum(sum(q^(n^2+n*m+m^2+n+m), m, -20, 20), n, -20, 20)`. Then `etamake(cq, 100)`.

b(q) workaround (avoids ω): `bq := 3*sum(sum(q^(3*(n^2+3*n*m+3*m^2)), m, -10, 10), n, -10, 10) - aq`. Then `etamake(bq, 100)`.

Expected: a(q) and c(q) are eta products. b(q) = η(τ)³/η(3τ) or similar.

**Exercise 5** (Slater (46)):
```
x := sum(q^(n*(3*n+1)/2)*aqprod(-q,q,n,50)/aqprod(q,q,2*n+1,50), n, 0, 10)
x := series(x, 50)
jp := jacprodmake(x, 50)
jac2series(jp, 500)
```

**Exercise 6** (homogeneous relations between a,b,c,a(q³),b(q³),c(q³)):
Define a(q), b(q), c(q) from Exercise 4, then compute their q³-substitutions using subs_q:
```
aq3 := subs_q(aq, 3)
bq3 := subs_q(bq, 3)
cq3 := subs_q(cq, 3)
findhom([aq, bq, cq, aq3, bq3, cq3], 2, 0)
```
If b(q) is not feasible, try just a(q) and c(q) variants.

**Exercise 7** (UE(q,3,7) in terms of C1,C2,C3):
```
C1 := etaq(7,50)^7/etaq(1,50)
C2 := etaq(1,50)^3*etaq(7,50)^3
C3 := etaq(1,50)^7/etaq(7,50)
f := sum(sum(legendre(m,7)*n^2*q^(m*n), n, 1, 50), m, 1, 50)
findhomcombo(f, [C1,C2,C3], 3, 0)
```
Note: k=3 means n^(k-1) = n^2.

**Exercise 8** (T as polynomial in ξ):
```
xi := q^2*etaq(49,100)/etaq(1,100)
TT := q*(etaq(7,100)/etaq(1,100))^4
TT6 := (etaq(5,100)/etaq(1,100))^6
findnonhomcombo(TT6, [xi], [1, 25], 0)
```
Wait — Exercise 8 defines T = (η(5τ)/η(τ))^6 which is (etaq(5)/etaq(1))^6. Actually re-reading: Exercise 8 says ξ = η(25τ)/η(τ) and T = (η(5τ)/η(τ))^6. Use `findnonhomcombo(TT, [xi], [1, 25], 0)` to find T as polynomial in ξ.

Actually, looking at it more carefully from block 23's pattern: the exercise asks to express T as a polynomial in ξ where T = (η(5τ)/η(τ))^6 and ξ = q*η(25τ)/η(τ). Try:
```
xi := q*etaq(25,100)/etaq(1,100)
TT := (etaq(5,100)/etaq(1,100))^6
findnonhomcombo(TT, [xi], [1, 25], 0)
```

**Exercise 9** (N(q) in terms of a(q), x(q)):
```
Nq := 1 - 504*sum(n^5*q^n/(1-q^n), n, 1, 50)
```
Wait — the sum needs to be a series. `n^5*q^n/(1-q^n) = n^5 * sum(q^(n*k), k, 1, ∞)`. Compute as: `Nq := 1 - 504*sum(sigma(n,5)*q^n, n, 1, 50)` where sigma(n,5) is the divisor sum. We have `sigma(n)` and `sigma(n,k)` built-ins. Actually check: `Nq := series(1 - 504*sum(sigma(n,5)*q^n, n, 1, 50), 50)`.

Then define x(q) = c(q)^3/a(q)^3 from Exercise 4:
```
xq := cq^3/aq^3
findnonhomcombo(Nq, [aq, xq], [1, 3], 0)
```
Note: a(q) has weight 1, c(q) has weight 1, so x(q) = c^3/a^3 is weight 0. N(q) has weight 6. The combo should express N(q) in terms of a(q)^6 * polynomial_in_x.

**Exercise 10** (findpoly for y = c³/a³ as function of m):
```
m := theta3(q,100)^2/theta3(q^3,40)^2
```
y is from Exercise 4/Block 25. Then:
```
findpoly(m, y, 3, 1, 60)
```

**Exercise 11** (sift pd(5n+r)):
```
PD := etaq(2, 200)/etaq(1, 200)
PD0 := sift(PD, 5, 0, 199)
jp0 := jacprodmake(PD0, 50)
jac2prod(jp0)
PD1 := sift(PD, 5, 1, 199)
jp1 := jacprodmake(PD1, 50)
jac2prod(jp1)
PD2 := sift(PD, 5, 2, 199)
jp2 := jacprodmake(PD2, 50)
jac2prod(jp2)
PD3 := sift(PD, 5, 3, 199)
jp3 := jacprodmake(PD3, 50)
jac2prod(jp3)
PD4 := sift(PD, 5, 4, 199)
jp4 := jacprodmake(PD4, 50)
jac2prod(jp4)
```

**Exercise 12** (quintuple product + sifting mod 5 and 7):
```
EULER := etaq(1, 500)
E0 := sift(EULER, 5, 0, 499)
E1 := sift(EULER, 5, 1, 499)
E2 := sift(EULER, 5, 2, 499)
E3 := sift(EULER, 5, 3, 499)
E4 := sift(EULER, 5, 4, 499)
```
For each Ek, use `jacprodmake` and `jac2prod`. Then repeat for mod 7:
```
F0 := sift(EULER, 7, 0, 499)
```
etc.

**Exercise 13** (Winquist):
Part (i): Use tripleprod to verify the product form.
Part (ii): `A0B0 := A0 * (tripleprod(q^14,q^33,200) - q*tripleprod(q^8,q^33,200))` then compute `A0B0 - q^3*A7*B4` and jacprodmake it. The Q(k) expansions from Block 37 are needed.

For each exercise, run the commands through the REPL, capture the output, and include it in the solutions file. Document any exercise that cannot be completed (e.g., parts requiring ω) with an explanation.

Format each exercise as:
```markdown
## Exercise N: [title]

### Commands
\`\`\`
[qseries3 commands]
\`\`\`

### Output
\`\`\`
[actual output]
\`\`\`

### Notes
[any mathematical observations or limitations]
```
  </action>
  <verify>
1. `exercises_solutions.md` exists and covers all 13 exercises.
2. Each exercise has qseries3 commands and output (or documented limitation).
3. At least 10 of 13 exercises produce meaningful computational results.
4. Exercise 4 (b(q)) workaround is attempted even if partial.
  </verify>
  <done>exercises_solutions.md created with all 13 exercises. Each has commands, output, and notes. At least 10 exercises produce correct computational results.</done>
</task>

</tasks>

<verification>
1. `bash tests/maple-checklist.sh` runs all 41 blocks: 30+ pass, ≤5 skip, remainder documented.
2. `maple_checklist.md` has every block marked `[x]`, `[~]` (limitation), or `[ ]` with notes.
3. `exercises_solutions.md` covers all 13 exercises with qseries3 commands and output.
4. No regressions: `bash tests/acceptance.sh` still passes.
</verification>

<success_criteria>
- All 41 Maple blocks attempted and status documented
- 30+ blocks pass (out of ~36 feasible, excluding 5 symbolic/infeasible)
- All 13 exercises attempted with qseries3 commands
- Three artifacts produced: test script, updated checklist, exercise solutions
</success_criteria>

<output>
After completion, create `.planning/phases/62-maple-checklist/62-02-SUMMARY.md`
</output>
