---
phase: 66-exercise-solutions-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/acceptance-exercises.sh]
autonomous: true

must_haves:
  truths:
    - "Exercise 4: etaq(1,50)^3/etaq(3,50) produces series starting 1 - 3q and etamake identifies it"
    - "Exercise 9: findnonhomcombo(N, [a,x], [6,2], 0) produces coefficients matching a^6(1+20x-8x^2)"
    - "Exercise 10: findpoly(m, y, 6, 1) produces a polynomial relating theta quotient m and y=c^3/a^3"
    - "All 10 acceptance tests pass"
    - "All 6 acceptance-powfrac tests pass"
    - "All 33 maple-checklist tests pass with 0 failures"
  artifacts:
    - path: "tests/acceptance-exercises.sh"
      provides: "Exercise 4, 9, 10 automated verification"
      contains: "Exercise 4"
  key_links:
    - from: "tests/acceptance-exercises.sh"
      to: "qseries.exe"
      via: "piped REPL commands"
      pattern: "run.*etaq.*etamake|findnonhomcombo|findpoly"
---

<objective>
Create an exercise verification test script and run the full regression suite to confirm all three exercises produce correct results and all existing tests still pass.

Purpose: This is the final verification gate for Milestone v4.2 (Fix Block Failures). Exercises 4, 9, and 10 from qseriesdoc.md depend on the q-shift fix (Phase 63), fractional power infrastructure (Phase 64), and Jacobi half-integer exponents (Phase 65). This plan confirms everything works together.
Output: tests/acceptance-exercises.sh (exercise verification script), documented regression results
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-exercise-solutions-regression/66-RESEARCH.md
@.planning/phases/63-q-shift-arithmetic-fix/63-01-SUMMARY.md
@.planning/phases/64-fractional-power-infrastructure/64-01-SUMMARY.md
@.planning/phases/65-jacobi-half-integer-exponents/65-01-SUMMARY.md
@tests/maple-checklist.sh
@tests/acceptance.sh
@tests/acceptance-powfrac.sh
@src/qfuncs.h
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exercise verification test script</name>
  <files>tests/acceptance-exercises.sh</files>
  <action>
Create `tests/acceptance-exercises.sh` following the exact same pattern as `tests/acceptance.sh` (binary discovery, run() helper, PASS/FAIL counters, exit code).

The script must test three exercises from qseriesdoc.md:

**Exercise 4 — b(q) = η(τ)³/η(3τ) (EX-01):**
Two subtests:
1. Compute `b := etaq(1,50)^3 / etaq(3,50)` then `series(b, 20)`. Verify output contains "1 - 3q" (the known first coefficients of the cubic AGM function b(q)).
2. Run `etamake(b, 50)` on the same b(q). Verify the output identifies an eta product (grep for η or eta or exponent pattern — the exact output format should match etamake's existing display). Use a flexible grep pattern like `(1-q` or `eta` since etamake prints product-of-eta notation.

**Exercise 9 — N(q) via findnonhomcombo (EX-02):**
This is the most complex exercise. All commands must be piped into a SINGLE qseries process so variables persist. Use `set_trunc(100)` for sufficient terms.

Commands to pipe:
```
set_trunc(100)
a := theta3(q,100)*theta3(q^3,100) + theta2(q,100)*theta2(q^3,100)
c3 := 27*q*etaq(3,100)^9/etaq(1,100)^3
x := c3/a^3
N := 1 - 504*sum(sigma(n,5)*q^n, n, 1, 99)
findnonhomcombo(N, [a, x], [6, 2], 0)
```

Expected result: findnonhomcombo should find that N = a^6*(1 + 20*x - 8*x^2). The output format uses X[1] for a and X[2] for x, so look for coefficients "20" and "-8" in the output (grep for "20" and the output containing X[1] and X[2] patterns).

If the computation takes too long or fails, document the failure message. The requirement says "attempted and documented even if infeasible." But research indicates T=100 IS feasible (~30s). Set a generous timeout and capture output regardless.

Verification grep: Check that the output contains both "20" and "8" (the key non-trivial coefficients from the cubic identity a^6(1+20x-8x^2)).

**Exercise 10 — findpoly cubic relation (EX-03):**
All commands piped into a single qseries process:

```
set_trunc(100)
m := theta3(q,100)^2 / theta3(q^3,100)^2
a := theta3(q,100)*theta3(q^3,100) + theta2(q,100)*theta2(q^3,100)
c3 := 27*q*etaq(3,100)^9/etaq(1,100)^3
y := c3/a^3
findpoly(m, y, 6, 1)
```

Expected result: A polynomial P(X,Y) where Y*(X^2+6X-3)^3 - 27*(X-1)*(X+1)^4 = 0. The findpoly output uses X for m and Y for y. Verify the output contains polynomial terms — grep for "X" and "Y" in the output (the polynomial should reference both variables).

**Script structure requirements:**
- Shebang: `#!/usr/bin/env bash`
- Binary discovery: same 4-path chain as acceptance.sh (dist/qseries.exe, dist/qseries, ./qseries.exe, ./qseries)
- `run()` helper: `printf '%s\n' "$@" | "$BIN" 2>&1`
- PASS/FAIL/SKIP counters with pass()/fail()/skip() functions
- Print summary at end: "Exercise tests: $PASS pass, $FAIL fail"
- Exit code: `[ "$FAIL" -eq 0 ]`
- Make the script executable: `chmod +x tests/acceptance-exercises.sh`
  </action>
  <verify>
Build the binary: `g++ -std=c++20 -O2 -o qseries.exe src/main.cpp && cp qseries.exe dist/qseries.exe`

Run: `bash tests/acceptance-exercises.sh`

All exercise tests should pass. Exercise 9 may take 30-60 seconds — wait for completion.
  </verify>
  <done>
tests/acceptance-exercises.sh exists with tests for all three exercises. Each exercise has at least one passing test. Exercise 9 either passes or is documented as attempted with output captured.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full regression suite verification</name>
  <files>tests/acceptance-exercises.sh</files>
  <action>
Run ALL existing test suites to confirm zero regressions after Phases 63-65 changes. Execute each suite sequentially and capture results:

1. Build fresh binary:
   ```
   g++ -std=c++20 -O2 -o qseries.exe src/main.cpp
   cp qseries.exe dist/qseries.exe
   ```

2. Run core acceptance tests:
   ```
   bash tests/acceptance.sh
   ```
   Expected: 10/10 PASS

3. Run fractional power tests:
   ```
   bash tests/acceptance-powfrac.sh
   ```
   Expected: 6/6 PASS

4. Run maple checklist:
   ```
   bash tests/maple-checklist.sh
   ```
   Expected: 33 PASS, 0 FAIL, 8 SKIP

5. Run exercise tests (created in Task 1):
   ```
   bash tests/acceptance-exercises.sh
   ```
   Expected: All exercises verified

If ANY test fails that previously passed, investigate and fix. The fix scope is limited to test script adjustments — no source code changes should be needed (research confirms all features already work).

After all suites pass, no further action needed — the SUMMARY will document final counts.
  </action>
  <verify>
All four test suites produce zero failures:
- `bash tests/acceptance.sh` → 10/10 PASS
- `bash tests/acceptance-powfrac.sh` → 6/6 PASS
- `bash tests/maple-checklist.sh` → 33 PASS, 0 FAIL, 8 SKIP
- `bash tests/acceptance-exercises.sh` → all PASS
  </verify>
  <done>
All test suites pass with zero failures. The complete test matrix:
- acceptance.sh: 10/10
- acceptance-powfrac.sh: 6/6
- maple-checklist.sh: 33/41 (33 pass, 0 fail, 8 skip)
- acceptance-exercises.sh: exercises 4, 9, 10 verified
No regressions from Phases 63-65 changes.
  </done>
</task>

</tasks>

<verification>
1. `tests/acceptance-exercises.sh` exists and is executable
2. Exercise 4 test verifies b(q) coefficients and etamake identification
3. Exercise 9 test attempts findnonhomcombo with E6 and cubic AGM functions
4. Exercise 10 test verifies findpoly cubic relation
5. All existing test suites produce identical pass/fail counts to baseline
</verification>

<success_criteria>
- tests/acceptance-exercises.sh produces 0 failures for all three exercises
- bash tests/acceptance.sh → 10/10 PASS
- bash tests/acceptance-powfrac.sh → 6/6 PASS
- bash tests/maple-checklist.sh → 33 PASS, 0 FAIL, 8 SKIP
- Zero regressions from any prior phase
</success_criteria>

<output>
After completion, create `.planning/phases/66-exercise-solutions-regression/66-01-SUMMARY.md`
</output>
