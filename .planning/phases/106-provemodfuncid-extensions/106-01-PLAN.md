---
phase: 106-provemodfuncid-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theta_ids.h
  - src/repl.h
  - tests/acceptance-theta-ids.sh
autonomous: true

must_haves:
  truths:
    - "User can call provemodfuncidBATCH(jacids, N) to prove multiple jacid identities on Gamma_1(N)"
    - "At least 2 theta_aids identities verified (trivial RR + constant)"
    - "tests/acceptance-theta-ids.sh passes (all 9 tests)"
    - "provemodfuncid single-identity flow unchanged; tests 7 and 8 still pass"
  artifacts:
    - path: src/theta_ids.h
      provides: provemodfuncidBATCH function
      contains: "provemodfuncidBATCH"
    - path: src/repl.h
      provides: REPL dispatch and help for provemodfuncidBATCH
      contains: "provemodfuncidBATCH"
  key_links:
    - from: src/repl.h
      to: src/theta_ids.h
      via: "provemodfuncidBATCH(jacids, N) call"
      pattern: "provemodfuncidBATCH\(jacids"
    - from: tests/acceptance-theta-ids.sh
      to: "provemodfuncidBATCH REPL"
      via: "echo input | qseries"
      pattern: "provemodfuncidBATCH"
---

<objective>
Implement provemodfuncidBATCH and theta_aids regression verification. User can prove multiple Jacobi theta identities in one call; at least 2 theta_aids identities (trivial RR + constant) verified; acceptance-theta-ids.sh passes; no regressions in provemodfuncid.

Purpose: GAP11-04 (provemodfuncidBATCH), GAP11-05 (theta_aids regression). Batch proving enables systematic identity verification without REPL round-trips.

Output: provemodfuncidBATCH in theta_ids.h, REPL dispatch in repl.h, Test 9 in acceptance-theta-ids.sh.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-provemodfuncid-extensions/106-RESEARCH.md

Reference implementations:
- src/theta_ids.h lines 315–384 (provemodfuncid, ProveModfuncIdResult)
- src/eta_cusp.h lines 224–235 (provemodfuncGAMMA0idBATCH pattern)
- src/repl.h lines 1605–1637 (provemodfuncGAMMA0idBATCH dispatch)
- src/repl.h lines 1638–1665 (provemodfuncid dispatch and jacid parser)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement provemodfuncidBATCH in theta_ids.h</name>
  <files>src/theta_ids.h</files>
  <action>
Add provemodfuncidBATCH after provemodfuncid (before #endif). Signature:

```cpp
inline std::vector<ProveModfuncIdResult> provemodfuncidBATCH(
    const std::vector<std::vector<std::pair<Frac, std::vector<JacFactor>>>>& jacids, int N) {
    std::vector<ProveModfuncIdResult> out;
    for (const auto& jacid : jacids)
        out.push_back(provemodfuncid(jacid, N));
    return out;
}
```

Do NOT change provemodfuncid's signature or implementation.
</action>
  <verify>grep -n "provemodfuncidBATCH" src/theta_ids.h; make -j4 2>&1 | tail -5</verify>
  <done>provemodfuncidBATCH declared and defined in theta_ids.h; build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Add REPL dispatch and help for provemodfuncidBATCH</name>
  <files>src/repl.h</files>
  <action>
1. Add help entry in the builtins vector (near line 494, after provemodfuncid):
   {"provemodfuncidBATCH", {"provemodfuncidBATCH(jacids, N)", "batch prove multiple Jacobi theta identities on Gamma_1(N)"}}

2. Add dispatch block immediately after the provemodfuncid block (before "partition"). Follow provemodfuncGAMMA0idBATCH pattern:
   - if (name == "provemodfuncidBATCH")
   - Expect args.size() == 2; error "expects provemodfuncidBATCH(jacids, N)"
   - args[0] must be List; outer loop over listExpr->elements (each is a jacid)
   - Each jacid is a List; inner loop reuses provemodfuncid's term parser: [coeff] or [coeff, [a,b,exp], ...]; JacFactor = [a,b,exp]
   - Build std::vector&lt;std::vector&lt;std::pair&lt;Frac, std::vector&lt;JacFactor&gt;&gt;&gt;&gt; jacids
   - N = evi(1)
   - results = provemodfuncidBATCH(jacids, N)
   - For each result: std::cout &lt;&lt; "id[" &lt;&lt; (i+1) &lt;&lt; "] sturm_bound=" &lt;&lt; results[i].sturm_bound &lt;&lt; " proven=" &lt;&lt; results[i].proven &lt;&lt; " " &lt;&lt; results[i].message &lt;&lt; std::endl
   - all_proven = 1 if every results[i].proven == 1 else 0
   - return static_cast&lt;int64_t&gt;(all_proven)

Copy the term-parsing logic from provemodfuncid (lines 1644–1661) into the inner loop over each jacid's elements.
</action>
  <verify>grep -n "provemodfuncidBATCH" src/repl.h; make -j4 2>&1 | tail -5</verify>
  <done>provemodfuncidBATCH callable from REPL; help shows signature</done>
</task>

<task type="auto">
  <name>Task 3: Add provemodfuncidBATCH test to acceptance-theta-ids.sh</name>
  <files>tests/acceptance-theta-ids.sh</files>
  <action>
Add Test 9 after Test 8 (before the "echo Total" block). Use provemodfuncidBATCH with 2 identities:
- id1: trivial RR form [[1,[5,25,-1],[10,25,1]],[-1,[5,25,-1],[10,25,1]]]
- id2: constant [[1],[-1]]

Command: provemodfuncidBATCH([[[1,[5,25,-1],[10,25,1]],[-1,[5,25,-1],[10,25,1]]],[[1],[-1]]], 25)

Expect: id[1] and id[2] both show proven=1. Use one check() with desc "provemodfuncidBATCH 2 identities" and pattern "id\[2\].*proven=1" — matching id[2] proves we got 2 results and the second identity passed.
</action>
  <verify>bash tests/acceptance-theta-ids.sh 2>&1</verify>
  <done>Test 9 passes; full script reports 9 passed, 0 failed</done>
</task>

</tasks>

<verification>
1. Run tests/acceptance-theta-ids.sh — all 9 tests pass
2. Manually: provemodfuncid([[1,[5,25,-1],[10,25,1]],[-1,[5,25,-1],[10,25,1]]], 25) still returns proven=1 (test 7)
3. Manually: provemodfuncid([[1],[-1]], 5) still returns proven=1 (test 8)
</verification>

<success_criteria>
- provemodfuncidBATCH(jacids, N) exists in theta_ids.h
- REPL accepts provemodfuncidBATCH and prints id[i] sturm_bound=... proven=... per identity
- acceptance-theta-ids.sh passes with 9/9
- provemodfuncid single-identity flow unchanged; tests 7 and 8 pass
</success_criteria>

<output>
After completion, create .planning/phases/106-provemodfuncid-extensions/106-01-SUMMARY.md
</output>
