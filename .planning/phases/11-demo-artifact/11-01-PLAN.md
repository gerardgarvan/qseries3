---
phase: 11-demo-artifact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/garvan-demo.sh
  - demo/README.md
  - Makefile
autonomous: true

must_haves:
  truths:
    - "demo/garvan-demo.sh exists and runs qseries commands"
    - "User can run make demo or bash demo/garvan-demo.sh; output streams to stdout"
    - "Script structure supports Phases 12–15 appending section blocks"
  artifacts:
    - path: demo/garvan-demo.sh
      provides: Runnable demo script with BIN detection and run helper
      contains: "run()", "printf", "BIN"
    - path: demo/README.md
      provides: Brief intro and run instructions
    - path: Makefile
      provides: demo target
  key_links:
    - from: demo/garvan-demo.sh
      to: qseries binary
      via: BIN variable and run() pipe
      pattern: "printf.*\\|.*BIN"
    - from: Makefile
      to: demo/garvan-demo.sh
      via: demo target
      pattern: "demo.*garvan-demo"
---

<objective>
Create demo/garvan-demo.sh with BIN detection and run() helper (mirroring tests/acceptance.sh), one minimal Rogers-Ramanujan section block, and section headers for Phases 12–15. Add demo target to Makefile. Create brief demo/README.md with run instructions.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-demo-artifact/11-CONTEXT.md
@.planning/phases/11-demo-artifact/11-RESEARCH.md
@tests/acceptance.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo/garvan-demo.sh</name>
  <files>demo/garvan-demo.sh</files>
  <action>
Create demo/garvan-demo.sh:

1. Shebang and set -e
2. BIN detection: BIN="./qseries.exe"; [ -f qseries.exe ] || BIN="./qseries"; [ -f "$BIN" ] || { echo "error: qseries binary not found"; exit 1; }
3. run() helper: run() { printf '%s\n' "$@" | "$BIN" 2>&1; }
4. One section: Rogers-Ramanujan (qseriesdoc §3.1)
   - echo "" and echo "--- Rogers-Ramanujan ---" for readability
   - run "rr := sum(q^(n^2)/aqprod(q,q,n,50), n, 0, 8)" "prodmake(rr, 40)"
5. Comment section headers as placeholders for Phases 12–15: # === Product conversion (§3.2–3.4) ===, # === Relations (§4) ===, # === Sifting and product identities (§5, §6) ===
</action>
  <verify>bash demo/garvan-demo.sh from project root streams output; no assertions; REPL banner and output visible.</verify>
  <done>garvan-demo.sh runs; Rogers-Ramanujan sum and prodmake output appear.</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile demo target and demo/README.md</name>
  <files>Makefile, demo/README.md</files>
  <action>
1. Add to Makefile (after acceptance target):
   demo: qseries.exe
       ./demo/garvan-demo.sh

2. Create demo/README.md with:
   - One-line intro: Garvan demo runs qseries commands from qseriesdoc.
   - Run instructions: make demo or bash demo/garvan-demo.sh (from project root)
</action>
  <verify>make demo runs demo script; README exists with run instructions.</verify>
  <done>make demo works; demo/README.md documents how to run.</done>
</task>

</tasks>

<verification>
- make demo runs and produces output
- bash demo/garvan-demo.sh works from project root
- Script structure matches 11-RESEARCH patterns
</verification>

<success_criteria>
- demo/garvan-demo.sh exists, runnable, one Rogers-Ramanujan block
- Makefile has demo target
- demo/README.md with run instructions
- Structure supports Phases 12–15 appending sections
</success_criteria>

<output>
After completion, create .planning/phases/11-demo-artifact/11-01-SUMMARY.md
</output>
