---
phase: 109-help-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/repl.h]
autonomous: true

must_haves:
  truths:
    - "help(prodmake) and help(etamake) include 1–2 examples"
    - "Help output has SYNOPSIS (signature + description); optionally SEE ALSO"
    - "Examples use qseries REPL syntax (sum, aqprod with T), sourced from qseriesdoc"
    - "Tab completion and help() listing still work (no regressions)"
  artifacts:
    - path: src/repl.h
      provides: HelpEntry struct, formatHelpEntry, getHelpTable with extended entries
      contains: "struct HelpEntry"
  key_links:
    - from: dispatchBuiltin("help")
      to: formatHelpEntry
      via: call when help(func) finds entry
      pattern: formatHelpEntry
    - from: formatHelpEntry
      to: SYNOPSIS/EXAMPLES/SEE ALSO output
      via: prints sig, examples, seeAlso when present
---

<objective>
Extend the qseries help system so per-function help includes examples and clearer structure (SYNOPSIS, EXAMPLES, SEE ALSO). Users get 1–2 runnable examples for key built-ins (prodmake, etamake, jacprodmake, aqprod, etaq, theta) sourced from qseriesdoc with Maple→REPL syntax translation.

Purpose: Per HELP-01–HELP-03, help(func) should be more useful than sig+one-line-desc. Maple-style CALLING SEQUENCE/SYNOPSIS/EXAMPLES/SEE ALSO parity.

Output: src/repl.h updated with HelpEntry struct, formatHelpEntry(), extended getHelpTable, updated help branch.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/109-help-extensions/109-RESEARCH.md
@qseriesdoc.md
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HelpEntry struct and formatHelpEntry</name>
  <files>src/repl.h</files>
  <action>
1. Add struct HelpEntry before getHelpTable (around line 463):
   - std::string sig;
   - std::string desc;
   - std::vector&lt;std::string&gt; examples;  // empty = none
   - std::vector&lt;std::string&gt; seeAlso;   // empty = none

2. Add inline void formatHelpEntry(const std::string& name, const HelpEntry& e):
   - Print SYNOPSIS: ansi::gold() + e.sig + ansi::reset() + " — " + e.desc
   - If e.examples non-empty: print "  EXAMPLES" then each line as "  qseries> " + example
   - If e.seeAlso non-empty: print "  SEE ALSO  " + comma-separated list (ansi::dim for names optional)

3. Change getHelpTable() return type from map&lt;string, pair&lt;sig, desc&gt;&gt; to map&lt;string, HelpEntry&gt;.

4. Migrate all existing entries: each entry becomes HelpEntry{sig, desc, {}, {}} (empty examples/seeAlso).

5. Update the help(func) branch in dispatchBuiltin (around 2189): replace the single-line output with:
   formatHelpEntry(fn, it->second);

6. Update any other consumers: getCompletionCandidates uses keys only (no change). handleTabCompletion uses count() only (no change). Levenshtein fallback uses keys only (no change). No other .first/.second access on help table values.

Verify build and that help(prodmake) still prints sig + desc (no examples yet).
  </action>
  <verify>g++ -std=c++20 -O2 -o qseries main.cpp 2>&1; echo "---"; echo "help(prodmake)" | ./qseries 2>&1 | head -5</verify>
  <done>Build succeeds; help(prodmake) shows SYNOPSIS line; no regression on help() or tab completion</done>
</task>

<task type="auto">
  <name>Task 2: Add examples and SEE ALSO for key built-ins</name>
  <files>src/repl.h</files>
  <action>
Add examples and seeAlso to these entries in getHelpTable (use qseriesdoc §3.1–3.4, translate Maple→REPL):

- prodmake: examples from §3.1 Rogers-Ramanujan:
  - "rr := sum(q^(n^2)/aqprod(q,q,n,50), n, 0, 8)"
  - "prodmake(rr, 40)"
  seeAlso: {"etamake", "jacprodmake"}

- etamake: examples from §3.3 theta→eta (qseriesdoc). Use theta3/theta4 which map to eta products:
  - "etamake(theta3(100), 100)"
  - "etamake(theta4(100), 100)"
  seeAlso: {"prodmake", "etaq", "jacprodmake"}

- jacprodmake: example from §3.4 RR→JAC:
  - "rr := sum(q^(n^2)/aqprod(q,q,n,50), n, 0, 8)"
  - "jacprodmake(rr, 40)"
  seeAlso: {"prodmake", "etamake", "jac2prod"}

- aqprod: example from §2.1.1:
  - "aqprod(q,q,5,50)"   // (q;q)_5
  seeAlso: {"etaq", "prodmake"}

- etaq: example from §2.2.1:
  - "etaq(1, 20)"   // partition GF 1/(q;q)_∞ to O(q^20)
  seeAlso: {"etamake", "aqprod"}

- theta2 (or theta3): example from §2.2.2:
  - "theta3(20)" or "theta2(20)"
  seeAlso: {"etaq", "etamake", "theta", "theta4"}

Syntax rules: Use sum(expr, var, lo, hi) not add(expr, var=lo..hi). Include T in aqprod(a,q,n,T). Examples must be self-contained or show setup+call. Each example is one line (no multi-line in string).
  </action>
  <verify>echo "help(prodmake)" | ./qseries 2>&1; echo "---"; echo "help(etamake)" | ./qseries 2>&1</verify>
  <done>help(prodmake) and help(etamake) show EXAMPLES and SEE ALSO; examples use valid REPL syntax</done>
</task>

</tasks>

<verification>
1. Run: echo "help(prodmake)" | ./qseries — must show SYNOPSIS, 2 examples, SEE ALSO
2. Run: echo "help(etamake)" | ./qseries — must show SYNOPSIS, examples, SEE ALSO
3. Run: echo "help(aqprod)" | ./qseries — must show at least one example
4. Run: echo "help" | ./qseries — must list all built-ins (no regression)
5. Pipe a prodmake example to qseries — must execute without error
</verification>

<success_criteria>
- HELP-01: prodmake, etamake, jacprodmake, aqprod, etaq, theta2 (or theta3) have 1–2 examples
- HELP-02: Help output has SYNOPSIS; SEE ALSO present for key built-ins
- HELP-03: Examples sourced from qseriesdoc, Maple syntax translated to REPL
- Tab completion and help() unchanged
</success_criteria>

<output>
After completion, create .planning/phases/109-help-extensions/109-01-SUMMARY.md
</output>
