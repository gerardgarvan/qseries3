---
phase: 89-list-indexing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/parser.h
  - src/repl.h
  - tests/maple-checklist.sh
autonomous: true

must_haves:
  truths:
    - "User can assign findhom/findnonhom result to a variable"
    - "User can index into RelationKernelResult via x[1], x[n] (1-based)"
    - "Indexed result displays as single relation via formatRelation"
    - "Out-of-range index throws clear 'index out of range' error"
    - "Maple checklist Block 21 passes (EQNS[1] works)"
  artifacts:
    - path: src/parser.h
      provides: Subscript expression parsing
      contains: "Tag::Subscript"
    - path: src/repl.h
      provides: RelationKernelResult in EnvValue, assignment, subscript eval
      contains: "RelationKernelResult"
  key_links:
    - from: src/repl.h evalStmt
      to: env.env
      via: "RelationKernelResult stored when assign RHS is findhom/findnonhom result"
      pattern: "std::holds_alternative<RelationKernelResult>"
    - from: src/repl.h evalExpr
      to: RelationKernelResult
      via: "Subscript case: base → RelationKernelResult, index → int, basis[i-1]"
      pattern: "Tag::Subscript"
---

<objective>
Add 1-based list indexing for findhom/findnonhom results. User assigns `EQNS := findnonhom(...)` and indexes via `EQNS[1]`, `EQNS[n]`. Out-of-range throws clear error. Maple checklist Block 21 passes.

Purpose: Close GAP-IDX-01; enable Maple workflow parity for relation indexing.
Output: Working x[1] on RelationKernelResult; Block 21 real test instead of skip.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-list-indexing/89-CONTEXT.md

# Current implementation
- RelationKernelResult: { basis: vector<vector<Frac>>, monomialExponents }
- EnvValue = Series | vector<JacFactor> | Partition | Phi1Result — no RelationKernelResult
- Assignment throws for findhom/findnonhom result
- Parser: parsePrimary returns Var for IDENT; no subscript syntax
- formatRelation(coeffs, monomialExponents) displays one relation
</context>

<tasks>

<task type="auto">
  <name>Task 1: EnvValue and assignment</name>
  <files>src/repl.h</files>
  <action>
    Add RelationKernelResult to EnvValue variant (line ~176). In evalStmt assignment branch (~2525): when res holds RelationKernelResult, store it with `env.env[s->assignName] = std::get&lt;RelationKernelResult&gt;(res); return res;`. Update the throw message to include "findhom/findnonhom result" or similar. In saveSession: add a branch to skip RelationKernelResult (do not serialize; relations are typically recomputed).
    In evalExpr Var case (before getSeriesFromEnv): add branch `if (std::holds_alternative&lt;RelationKernelResult&gt;(ev-&gt;second)) return std::get&lt;RelationKernelResult&gt;(ev-&gt;second);` — so evaluating EQNS (base of EQNS[1]) returns the stored RelationKernelResult.
  </action>
  <verify>Build: `make` succeeds. Run `echo 'EQNS := findnonhom([theta3(q,50)/theta3(q^5,50), theta3(q^5,50)/theta3(q^25,50)], 2, 10)' | ./qseries.exe` — must NOT throw "assignment requires Series..."; output may show relations.
  </verify>
  <done>findhom/findnonhom result can be assigned to a variable; saveSession does not crash on RelationKernelResult.
  </done>
</task>

<task type="auto">
  <name>Task 2: Parser subscript syntax</name>
  <files>src/parser.h</files>
  <action>
    Add Expr::Tag::Subscript to the Tag enum. Use existing left (base) and right (index) fields for Subscript (same as BinOp). Add static Expr::makeSubscript(ExprPtr base, ExprPtr index) that sets tag=Subscript, left=base, right=index. In Parser::parseExpr, immediately after `ExprPtr left = parsePrimary();`, add a loop: while (peek().kind == Token::Kind::LBRACK) { consume(); ExprPtr idx = parseExpr(0); expect(Token::Kind::RBRACK); left = Expr::makeSubscript(std::move(left), std::move(idx)); } — so x[1], (a+b)[1], and findhom(...)[1] all parse correctly.
  </action>
  <verify>Build: `make` succeeds. Parse `EQNS[1]` — must produce Subscript(Var("EQNS"), IntLit(1)). Add a quick test or inspect: `echo 'x[1]' | ./qseries.exe` should parse (may error "undefined variable" at eval, which is expected).
  </verify>
  <done>x[1] and expr[1] parse as Subscript(expr, index).
  </done>
</task>

<task type="auto">
  <name>Task 3: Eval subscript and Block 21</name>
  <files>src/repl.h, tests/maple-checklist.sh</files>
  <action>
    In evalExpr, add case Expr::Tag::Subscript:
      1. Eval base: EvalResult baseRes = eval(e->left.get(), env, sumIndices);
      2. If not RelationKernelResult: throw "subscript only supported for findhom/findnonhom results";
      3. int64_t i = evalToInt(e->right.get(), env, sumIndices);
      4. If i &lt; 1 or i &gt; (int)arg.basis.size(): throw "index out of range";
      5. Build RelationKernelResult with single row: basis = { arg.basis[i-1] }, monomialExponents unchanged;
      6. Return it.
    Add evalExpr case for Tag::Subscript before the final throw. Use e->left for base, e->right for index.
    In tests/maple-checklist.sh: Replace Block 21 skip with a real test. Run: Fq, Fq5, U definitions; EQNS := findnonhom([Fq, Fq5, U], 3, 20); EQNS[1]. Expect output to contain "X" (relation format). Use run + grep pattern like Block 20.
  </action>
  <verify>`make` succeeds. `tests/maple-checklist.sh` — Block 21 passes (no longer skipped). Manual: `echo 'EQNS := findnonhom([theta3(q,50)/theta3(q^5,50), theta3(q^5,50)/theta3(q^25,50)], 2, 10); EQNS[1]' | ./qseries.exe` shows single relation. `echo 'EQNS := findnonhom([theta3(q,50)/theta3(q^5,50)], 1, 5); EQNS[99]' | ./qseries.exe` throws "index out of range".
  </verify>
  <done>EQNS[1] returns and displays the first relation; out-of-range throws clearly; maple-checklist Block 21 passes.
  </done>
</task>

</tasks>

<verification>
- `make` produces qseries.exe without errors
- `tests/maple-checklist.sh` — Block 21 shows pass (not skip)
- Manual: EQNS:=findnonhom(...); EQNS[1] displays one relation; EQNS[99] throws "index out of range"
- All existing maple-checklist blocks still pass (no regressions)
</verification>

<success_criteria>
1. GAP-IDX-01 satisfied: x[1] returns first element when x is findhom/findnonhom result
2. x[n] works for n in [1, size]; out-of-range throws "index out of range"
3. Maple checklist Block 21 passes with real test
</success_criteria>

<output>
After completion, create `.planning/phases/89-list-indexing/89-01-SUMMARY.md`
</output>
