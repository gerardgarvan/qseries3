---
phase: 96-block-4-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/maple-checklist.sh
autonomous: true

must_haves:
  truths:
    - "Block 4 in maple-checklist.sh runs real test (set_trunc(64), t8 := T(8,8), factor(t8)) instead of skip"
    - "Block 4 passes when output contains Φ (U+03A6) or Cyclotomic and contains q⁶ (cyclotomic product form per qseriesdoc Output (4))"
    - "Block 5 (qfactor(t8,20)) still runs and passes — no regression"
  artifacts:
    - path: tests/maple-checklist.sh
      provides: Block 4 factor(t8) cyclotomic test, Block 5 qfactor test unchanged
      contains: "Block 4: factor(t8)"
  key_links:
    - from: tests/maple-checklist.sh
      to: factor built-in (Phase 95)
      via: "run set_trunc(64); t8 := T(8,8); factor(t8)"
      pattern: "factor\(t8\)"
---

<objective>
Replace maple-checklist Block 4 skip with a real test. factor(t8) now produces cyclotomic output (Phase 95); Block 4 should verify that instead of skipping.

Purpose: BLOCK4-01; maple-checklist parity with qseriesdoc Output (4) for factor(t8).

Output: tests/maple-checklist.sh with Block 4 as pass/fail test (no skip).
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 96)
@qseriesdoc.md (lines 284-288: Output (4) factored into cyclotomic polynomials)
@tests/maple-checklist.sh (Block 3-5 structure, run/grep pattern)
@tests/acceptance-factor.sh (factor(t8) grep patterns: Φ|Cyclotomic, q⁶|q^6)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Block 4 skip with factor(t8) cyclotomic test</name>
  <files>tests/maple-checklist.sh</files>
  <action>
Replace Block 4 (lines 48-49):

Current:
  skip "Block 4: factor(t8) — Maple-only feature"

New:
  # Block 4: factor(t8) (§3.2)
  # Expected: cyclotomic product (q⁶·Φ₁²·Φ₂·Φ₃²·...) per qseriesdoc Output (4)
  OUT4=$(run "set_trunc(64)" "t8 := T(8,8)" "factor(t8)")
  if echo "$OUT4" | grep -qE 'Φ|Cyclotomic' && echo "$OUT4" | grep -qE 'q⁶|q\^6'; then
      pass "Block 4: factor(t8) cyclotomic"
  else
      fail "Block 4: factor(t8) cyclotomic"
  fi

Do NOT modify Block 5 (lines 52-57). Ensure Block 5 remains exactly as:
  if run "set_trunc(64)" "t8 := T(8,8)" "qfactor(t8, 20)" | grep -q "1-q"; then
      pass "Block 5: qfactor(t8)"
  ...
  fi

Use the same run/grep pattern as acceptance-factor.sh: Φ or Cyclotomic for cyclotomic notation, q⁶ or q^6 for q-power. Script must be UTF-8.
  </action>
  <verify>bash tests/maple-checklist.sh 2>&1 | head -60</verify>
  <done>Block 4 shows PASS (not SKIP); Block 5 shows PASS; exit 0 if no failures</done>
</task>

</tasks>

<verification>
1. Run: bash tests/maple-checklist.sh
2. Block 4 must show: PASS: Block 4: factor(t8) cyclotomic
3. Block 5 must show: PASS: Block 5: qfactor(t8)
4. SKIP count decreases by 1 vs current (Block 4 no longer skipped)
5. No new failures
</verification>

<success_criteria>
- Block 4 replaces skip with real test; passes when factor(t8) output contains Φ/Cyclotomic and q⁶
- Output style matches qseriesdoc Output (4) (cyclotomic factored form)
- Block 5 continues to pass — no regression
- maple-checklist.sh runs to completion with 0 failures for Blocks 4 and 5
</success_criteria>

<output>
After completion, create `.planning/phases/96-block-4-parity/96-01-SUMMARY.md`
</output>
