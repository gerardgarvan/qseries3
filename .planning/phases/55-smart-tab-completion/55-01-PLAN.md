---
phase: 55-smart-tab-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
autonomous: true

must_haves:
  truths:
    - "Multiple matches fill to longest common prefix before listing candidates"
    - "Completing a unique function name appends ( automatically"
    - "Completing a variable name does NOT append ("
    - "Existing Tab behavior for no-match cases is preserved"
    - "All existing acceptance tests still pass"
  artifacts:
    - path: "src/repl.h"
      provides: "Smart tab completion"
      contains: "lcp"
---

<objective>
Enhance handleTabCompletion in repl.h with two improvements:
1. LCP fill: when multiple candidates share a common prefix longer than what's typed, fill to that prefix before listing
2. Auto-parentheses: when completing a unique function name (from getHelpTable, not env vars), append `(`

Purpose: Faster, more intuitive tab completion.
Output: Modified `src/repl.h`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: LCP fill and auto-parentheses</name>
  <files>src/repl.h</files>
  <action>
Modify handleTabCompletion in repl.h.
  </action>
  <done>Smart tab completion implemented</done>
</task>

</tasks>
