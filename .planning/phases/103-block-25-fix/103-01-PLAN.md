---
phase: 103-block-25-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "maple-checklist Block 25 passes (findpoly on theta2/theta3 quotients; output contains X)"
    - "acceptance-exercises EX-10a passes (findpoly output contains coefficient 27)"
    - "acceptance-exercises EX-10b passes (findpoly output uses X variables)"
    - "q-shift alignment via addAligned/operator+ (Phase 97) remains intact"
  artifacts:
    - path: tests/maple-checklist.sh
      provides: Block 25 real test
      contains: "x1 + x2" and "findpoly(x, x, 3, 3)"
    - path: tests/acceptance-exercises.sh
      provides: EX-10a and EX-10b tests
      contains: "findpoly(m, y, 6, 1)" and grep for "27" and "X"
  key_links:
    - from: tests/maple-checklist.sh
      to: Block 25
      via: run "x1 := theta2(q,100)^2/theta2(q^3,40)^2" "x2 := theta3(q,100)^2/theta3(q^3,40)^2" "x := x1 + x2" "findpoly(x, x, 3, 3)"
      pattern: "Block 25"
    - from: Phase 97 addAligned/operator+
      to: findpoly
      via: x1+x2 uses operator+ which aligns; findpoly(x,x,3,3) receives aligned series
      pattern: "addAligned|operator\\+"
---

<objective>
Verification-first phase: run maple-checklist Block 25 and acceptance-exercises EX-10. If both pass, phase is verification-only (no code changes). If either fails, fix the regression without re-implementing addAligned or operator+ (Phase 97 work).
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-block-25-fix/103-CONTEXT.md
@.planning/phases/97-block-25-fix/97-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Block 25 and EX-10; verify both pass</name>
  <files></files>
  <action>
1. Build: `make` or `g++ -std=c++20 -O2 -o qseries main.cpp`
2. Run maple-checklist: `bash tests/maple-checklist.sh` (or `make acceptance-maple`)
3. Check Block 25: output must show "Block 25: findpoly theta2/theta3 quotients" as PASS
4. Run acceptance-exercises: `bash tests/acceptance-exercises.sh` (or `make acceptance-exercises`)
5. Check EX-10: EX-10a (grep 27) and EX-10b (grep X) must pass
6. If both Block 25 and EX-10 pass, phase is complete (verification-only). Skip Task 2. Create SUMMARY with status "verification-only, no code changes".
7. If either fails, proceed to Task 2.
  </action>
  <verify>
bash tests/maple-checklist.sh 2>&1 | grep -E "Block 25|PASS|FAIL"
bash tests/acceptance-exercises.sh 2>&1 | grep -E "EX-10a|EX-10b|pass|fail"
  </verify>
  <done>
Block 25 shows PASS; EX-10a and EX-10b show pass. Or failures documented for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix regression if Block 25 or EX-10 failed</name>
  <files>src/series.h, src/rr_ids.h, src/relations.h, src/repl.h</files>
  <action>
Execute ONLY if Task 1 found Block 25 or EX-10 failures. Do NOT re-implement addAligned or operator+ — Phase 97 work must be reused.

1. **Diagnose**: If Block 25 failed — trace x1+x2 (operator+) and findpoly(x,x,3,3). Check that addAligned is used when q_shifts differ; findpoly receives aligned series. If EX-10 failed — trace findpoly(m,y,6,1) path; EX-10 uses theta3 quotients and c3/a^3; alignment in `a` (sum of theta products) may matter.
2. **Fix**: Apply minimal fix — e.g. correct operator+ call site, findpoly input handling, or theta quotient evaluation path. Do NOT duplicate addAligned logic.
3. **Verify**: Re-run `bash tests/maple-checklist.sh` and `bash tests/acceptance-exercises.sh`. Block 25 and EX-10a/EX-10b must pass.
4. **Regression**: Run `make acceptance-all` (or equivalent) to confirm no regressions.
  </action>
  <verify>
bash tests/maple-checklist.sh 2>&1 | grep -E "Block 25|PASS|FAIL"
bash tests/acceptance-exercises.sh 2>&1 | grep -E "EX-10a|EX-10b"
make acceptance-all 2>&1 || true
  </verify>
  <done>
Block 25 passes; EX-10a and EX-10b pass; no regressions.
  </done>
</task>

</tasks>

<verification>
1. maple-checklist Block 25: PASS (findpoly(x,x,3,3) output contains "X")
2. acceptance-exercises EX-10a: pass (output contains coefficient 27)
3. acceptance-exercises EX-10b: pass (output uses X variables)
4. If fix applied: acceptance-all shows no new failures
</verification>

<success_criteria>
- maple-checklist Block 25 passes
- acceptance-exercises EX-10 (EX-10a, EX-10b) passes
- No re-implementation of addAligned or operator+ (Phase 97 work)
- GAP11-01 satisfied: findpoly on theta2/theta3 quotients works via q-shift alignment
</success_criteria>

<output>
After completion, create `.planning/phases/103-block-25-fix/103-01-SUMMARY.md`
</output>
