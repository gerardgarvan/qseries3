---
phase: 108-error-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/repl.h]
autonomous: true

must_haves:
  truths:
    - "Runtime errors follow Maple format: Error, (in funcname) message"
    - "Argument errors use expectArg helper with ordinals (1st, 2nd, 3rd)"
    - "At least 3 built-ins migrated to expectArg (etaq, prodmake, etamake)"
  artifacts:
    - path: src/repl.h
      provides: runtimeErr Maple format, ordinal, expectArg
      contains: ["Error, (in ", "expectArg", "ordinal"]
  key_links:
    - from: dispatchBuiltin throw sites
      to: runtimeErr or expectArg
      via: throw std::runtime_error(runtimeErr(name, msg)) or expectArg(N,name,expected,received)
---

<objective>
Implement Maple-style runtime error format and the expectArg argument-error helper. Change runtimeErr to return "Error, (in func) message"; add ordinal(N) and expectArg(N, name, expected, received); migrate etaq, prodmake, and etamake to use expectArg for their arity/type argument errors.
Purpose: ERR-03, ERR-04 — runtime and argument errors become actionable and Maple-familiar.
Output: Updated repl.h with Maple format and 3+ built-ins using expectArg.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-error-diagnostics/108-CONTEXT.md
@.planning/phases/108-error-diagnostics/108-RESEARCH.md
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maple format + ordinal + expectArg helpers</name>
  <files>src/repl.h</files>
  <action>
Replace runtimeErr (lines 49-51) to return Maple format:
  return func.empty() ? ("Error, " + msg) : ("Error, (in " + func + ") " + msg);

Add ordinal(N) inline helper immediately after runtimeErr:
  Returns "1st", "2nd", "3rd", "4th", ... per Maple. Use rule: 11th/12th/13th are "th"; else 1->st, 2->nd, 3->rd, else th. Handle n<=0 as fallback.

Add expectArg(N, name, expected, received) inline helper:
  return "invalid input: expects its " + ordinal(N) + " argument, " + name + ", to be " + expected + ", but received " + received;

Per CONTEXT: ordinals 1st/2nd/3rd; argument errors use "invalid input:" prefix. "Received Y" format: use short type-style labels per RESEARCH (e.g. "series", "integer") for clarity; literal for small values acceptable.
  </action>
  <verify>grep -n "Error, (in " src/repl.h | head -3; grep -n "expectArg\|ordinal" src/repl.h | head -5</verify>
  <done>runtimeErr returns Maple format; ordinal and expectArg exist and are used in subsequent task.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate etaq, prodmake, etamake to expectArg</name>
  <files>src/repl.h</files>
  <action>
Migrate these built-ins to use expectArg for arity/argument errors. Keep non-argument errors (e.g. "modulus cannot be zero") using runtimeErr(name, msg) — do NOT prepend "invalid input:" per CONTEXT discretion.

etaq: Change "expected etaq(k), etaq(k,T), or etaq(q,k,T), got N arguments" to expectArg when wrong arity. Use ordinal for argument position where applicable. Example: wrong first arg type -> expectArg(1, "k", "integer", "series").

prodmake: "expects 2 arguments" -> use expectArg(1, "f", "series", received) or expectArg(2, "T", "integer", received) when type is wrong; for arity use expectArg with appropriate position.

etamake: Same pattern — expectArg for type/arity errors.

Preserve ev/evi wrappers that rethrow with runtimeErr(name, e.what()) — those propagate nested errors; ensure e.what() from expectArg flows through correctly.

Do NOT migrate all built-ins; 3 is sufficient for this phase per "migrate incrementally" in CONTEXT.
  </action>
  <verify>make -C . 2>&1 | tail -5; echo 'etaq(1)' | ./qseries 2>&1 | head -3</verify>
  <done>etaq, prodmake, etamake use expectArg for their argument errors; build passes; etaq(1) shows Error, (in etaq) invalid input: ...</done>
</task>

</tasks>

<verification>
- Run `echo 'etaq(1)' | ./qseries 2>&1` — output must contain "Error, (in etaq)" and "invalid input" and ordinal (e.g. "1st").
- Run `echo 'prodmake(x,50)' | ./qseries 2>&1` with x undefined — must show Error, (in prodmake) with Maple format.
</verification>

<success_criteria>
- runtimeErr returns "Error, (in func) msg"
- expectArg produces "invalid input: expects its Nth argument, name, to be expected, but received received"
- etaq, prodmake, etamake use expectArg for argument/arity errors
- Build succeeds; no regressions in existing error paths
</success_criteria>

<output>
After completion, create .planning/phases/108-error-diagnostics/108-01-SUMMARY.md
</output>
