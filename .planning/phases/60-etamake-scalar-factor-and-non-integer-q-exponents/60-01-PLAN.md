---
phase: 60-etamake-scalar-factor-and-non-integer-q-exponents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/convert.h
  - src/repl.h
autonomous: true

must_haves:
  truths:
    - "etamake(theta2(100),100) displays '2 η(4τ)² / η(2τ)'"
    - "etamake(theta3(100),100) displays 'η(2τ)⁵ / (η(τ)² η(4τ)²)'"
    - "etamake(theta4(100),100) displays 'η(τ)² / η(2τ)'"
    - "All existing acceptance tests pass"
  artifacts:
    - path: "src/convert.h"
      provides: "etamake returns scalar factor via k=0 entry"
    - path: "src/repl.h"
      provides: "formatEtamake displays scalar and uses abs(exponent) in denominator"
---

<objective>
Fix two bugs in etamake display:

1. **Lost scalar factor**: etamake divides out leading coefficient but doesn't report it.
   Fix: prepend a {0, lead} entry in the result vector (k=0 convention for scalar).

2. **Wrong denominator exponents**: formatEtamake shows raw negative exponents in denominator.
   Fix: use abs(exponent) for denominator parts; skip superscript when abs(ex)==1.

Output: Modified `src/convert.h` and `src/repl.h`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: etamake returns scalar factor</name>
  <files>src/convert.h</files>
  <action>
In etamake(), after normalizing (line 36), if lead != 1, prepend {0, lead} to the result
vector before returning. k=0 is the convention for "scalar factor".
  </action>
  <done>etamake returns scalar factor as k=0 entry</done>
</task>

<task type="auto">
  <name>Task 2: Fix formatEtamake display</name>
  <files>src/repl.h</files>
  <action>
In formatEtamake():
1. Extract and remove k=0 entry (scalar factor) if present
2. For denominator entries: use abs(ex) instead of ex for the superscript
3. Skip superscript when abs(ex)==1 for both numerator and denominator
4. Prepend scalar factor to output if not 1
  </action>
  <done>formatEtamake displays correctly with scalar and absolute exponents</done>
</task>

</tasks>

<verification>
1. etamake(theta2(100),100) → "2 η(4τ)² / η(2τ)"
2. etamake(theta3(100),100) → "η(2τ)⁵ / (η(τ)² η(4τ)²)"
3. etamake(theta4(100),100) → "η(τ)² / η(2τ)"
4. All 10 acceptance tests pass (tests 3a/3b specifically test etamake)
</verification>
