---
phase: 58-docker
plan: 01
type: execute
wave: 1
depends_on: [57]
files_modified:
  - Dockerfile
  - .dockerignore
  - Makefile
  - .github/workflows/release.yml
autonomous: true

must_haves:
  truths:
    - "Dockerfile uses multi-stage build (gcc → scratch)"
    - "docker run -it launches interactive REPL"
    - "Image < 5MB"
    - "Makefile has docker-build and docker-run targets"
    - "CI pushes to ghcr.io on release"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage Docker build"
    - path: ".dockerignore"
      provides: "Build context exclusions"
---

<objective>
Create Docker infrastructure:
1. Multi-stage Dockerfile (gcc:14 build → scratch runtime)
2. .dockerignore for small build context
3. Makefile docker-build and docker-run targets
4. Add Docker push step to release workflow

Output: Dockerfile, .dockerignore, updated Makefile, updated release.yml
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
Create multi-stage Dockerfile:
- Stage 1 (build): FROM gcc:14-bookworm, COPY src/, g++ -std=c++20 -O2 -static
- Stage 2 (runtime): FROM scratch, COPY --from=build /qseries /qseries, ENTRYPOINT ["/qseries"]
Create .dockerignore excluding .git, .planning, build, dist, node_modules, *.exe
  </action>
  <done>Dockerfile and .dockerignore created</done>
</task>

<task type="auto">
  <name>Task 2: Makefile targets + CI Docker push</name>
  <files>Makefile, .github/workflows/release.yml</files>
  <action>
Add docker-build and docker-run targets to Makefile.
Add Docker build+push job to release.yml using docker/build-push-action@v6 pushing to ghcr.io.
  </action>
  <done>Makefile targets and CI Docker push added</done>
</task>

</tasks>
