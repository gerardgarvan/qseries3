---
phase: 48-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/ (deleted)
  - .github/workflows/deploy.yml (deleted)
  - Makefile
autonomous: true

must_haves:
  truths:
    - "website/ directory no longer exists"
    - ".github/workflows/deploy.yml no longer exists"
    - "wasm-website is no longer a Makefile target"
    - "Core build (make) still produces working qseries binary"
  artifacts:
    - path: "Makefile"
      provides: "Clean Makefile without wasm-website target"
      contains: "wasm:"
  key_links:
    - from: "Makefile"
      to: "build/wasm/"
      via: "wasm target still works independently"
      pattern: "make wasm"
---

<objective>
Remove all complex website infrastructure from the codebase: delete the entire website/ directory (Astro Starlight, xterm.js playground, KaTeX, node_modules), delete the GitHub Actions CI/CD workflow, and remove the wasm-website Makefile target. Keep the wasm build target intact.

Purpose: Clean slate before creating the simpler single-page HTML site.
Output: Smaller repo with no website/ directory, no CI/CD, clean Makefile.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete website directory and CI/CD workflow</name>
  <files>website/, .github/workflows/deploy.yml</files>
  <action>
Delete the entire `website/` directory recursively (contains Astro Starlight project, node_modules, playground, KaTeX config, all content pages).

Delete `.github/workflows/deploy.yml` (Cloudflare Pages deployment pipeline — no longer needed).

If `.github/workflows/` is now empty, delete `.github/` as well.

Use `rm -rf` via Cygwin bash for the deletions.
  </action>
  <verify>
1. `website/` directory does not exist
2. `.github/workflows/deploy.yml` does not exist
3. `ls website/` returns error (directory gone)
  </verify>
  <done>website/ and .github/workflows/deploy.yml deleted</done>
</task>

<task type="auto">
  <name>Task 2: Remove wasm-website Makefile target</name>
  <files>Makefile</files>
  <action>
Remove the `wasm-website` target from the Makefile (lines 111-114):

```makefile
wasm-website: build/wasm/qseries.js
	mkdir -p website/public/wasm
	cp build/wasm/qseries.js website/public/wasm/
	cp build/wasm/qseries.wasm website/public/wasm/
```

Keep the `wasm` target and `build/wasm` directory target intact — the Wasm build itself is still useful.

After editing, verify the core build still works by running `make` (or the g++ command directly).
  </action>
  <verify>
1. `grep wasm-website Makefile` returns no results
2. `grep "wasm:" Makefile` still returns the wasm target
3. Core build works: `make` or `g++ -std=c++20 -O2 -static -o qseries src/main.cpp` succeeds
  </verify>
  <done>wasm-website target removed from Makefile; core build still works</done>
</task>

</tasks>

<verification>
1. `website/` directory does not exist
2. `.github/workflows/deploy.yml` does not exist
3. `wasm-website` is not in the Makefile
4. `wasm` target is still in the Makefile
5. `make` produces a working qseries binary
</verification>

<success_criteria>
- All complex website infrastructure removed
- Core build unaffected
- Repo is clean for Phase 49 (single-page website creation)
</success_criteria>

<output>
After completion, create `.planning/phases/48-cleanup/48-01-SUMMARY.md`
</output>
