---
phase: 111-ergonomics-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
  - tests/acceptance-ergonomics.sh
autonomous: true

must_haves:
  truths:
    - "REPL continues prompting when parens/brackets unclosed (e.g. sum(1,2,\n until closed)"
    - "Trailing backslash at EOF in script mode does not hang (printf 'x := 1\\' | qseries exits)"
    - "All TTY features (banner, tab, history, clear, timing) guarded with stdin_is_tty() in script mode"
  artifacts:
    - path: src/repl.h
      provides: bracketsUnclosed helper, bracket continuation loop, EOF guard in script-mode continuation
      contains: bracketsUnclosed, std::cin.eof()
  key_links:
    - from: runRepl continuation logic
      to: bracketsUnclosed
      via: while (bracketsUnclosed(line)) after backslash loop
      pattern: bracketsUnclosed
    - from: script-mode getline branches (backslash and bracket)
      to: std::cin
      via: eof() check before getline
      pattern: std::cin.eof\(\)
---

<objective>
Implement ERGO-01 (bracket-aware multi-line), ERGO-02 (trailing backslash at EOF no hang), and ERGO-04 (TTY guards audit) per 111-RESEARCH.md.

Purpose: Users can type multi-line expressions naturally; scripts with trailing backslash exit; script mode never runs TTY-only features.
Output: bracketsUnclosed, bracket continuation loop, EOF guard, TTY audit verification, acceptance test.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111-ergonomics-polish/111-RESEARCH.md
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: bracketsUnclosed + bracket continuation (ERGO-01)</name>
  <files>src/repl.h</files>
  <action>
1. Add inline function `bracketsUnclosed(const std::string& s)` near the top of the `#ifndef __EMSCRIPTEN__` REPL section (before runRepl). Stack-based: depth_paren, depth_brack; count `(` `)` `[` `]`; return true if depth goes negative or any depth > 0 at end. Use unsigned char for iteration. Per 111-RESEARCH.md Algorithm.

2. Add bracket continuation loop after the backslash continuation loop (before `trimmed = trim(line)`). Order: backslash first, then bracket. While `bracketsUnclosed(line)` and contCount < maxContinuations: prompt `  > `; if TTY use readLineRaw, else use getline (script mode). Append " " + next to line. Break on read failure or empty line in script mode. Use same contCount or a separate bracketContCount (limit 100) to avoid infinite loop.
</action>
  <verify>
grep -n "bracketsUnclosed" src/repl.h
printf 'sum(1,\n2)\n' | ./qseries 2>&1 | head -5
Manual: run qseries, type "sum(1," Enter — should prompt "  > " until closing paren.
</verify>
  <done>REPL continues prompting when ( or [ unclosed; closing ) or ] submits.</done>
</task>

<task type="auto">
  <name>Task 2: EOF guard in script mode + TTY audit (ERGO-02, ERGO-04)</name>
  <files>src/repl.h, tests/acceptance-ergonomics.sh</files>
  <action>
1. In the backslash continuation loop, script-mode branch (else block): add `if (std::cin.eof()) break;` immediately before `std::getline(std::cin, next)`.

2. In the bracket continuation loop (from Task 1), script-mode branch: add the same `if (std::cin.eof()) break;` before getline.

3. TTY audit (ERGO-04): Confirm these paths are guarded by stdin_is_tty(): banner, loadHistory, readLineRaw (main + continuations), clear escape sequences, timing output, saveHistory. Per RESEARCH.md these exist; verify during implementation. Fix any missing guard. Document in commit message.

4. Create tests/acceptance-ergonomics.sh: (a) `printf 'x := 1\' | ./qseries 2>&1` — must exit without hanging (timeout 3s); (b) `printf 'sum(1,\n2)\n' | ./qseries 2>&1` — must run and show result. Exit 0 if pass.
</action>
  <verify>
grep -n "std::cin.eof()" src/repl.h
timeout 3 bash -c "printf 'x := 1\\\\' | ./qseries 2>&1" || true
bash tests/acceptance-ergonomics.sh
</verify>
  <done>Trailing backslash at EOF exits; bracket continuation works in script mode; TTY audit verified.</done>
</task>

</tasks>

<verification>
- bash tests/acceptance-ergonomics.sh
- make -j
- Manual: bracket continuation in TTY and script mode
</verification>

<success_criteria>
- ERGO-01: Unclosed ( or [ prompts "  > " until balanced
- ERGO-02: printf 'x := 1\' | qseries exits (no hang)
- ERGO-04: No TTY-only code runs when !stdin_is_tty()
</success_criteria>

<output>
After completion, create .planning/phases/111-ergonomics-polish/111-01-SUMMARY.md
</output>
