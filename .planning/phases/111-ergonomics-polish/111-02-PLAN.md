---
phase: 111-ergonomics-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
autonomous: true

must_haves:
  truths:
    - "Ctrl+C during raw mode restores termios before re-raise — shell remains usable"
  artifacts:
    - path: src/repl.h
      provides: SIGINT handler that restores termios, then raise(SIGINT)
      contains: repl_sigint_handler, sigaction, g_repl_orig_termios
  key_links:
    - from: runRepl
      to: repl_sigint_handler
      via: sigaction(SIGINT, &sa, &old) when stdin_is_tty() and POSIX
      pattern: sigaction.*SIGINT
---

<objective>
Implement ERGO-03: register SIGINT handler on POSIX/CYGWIN that restores original termios before re-raising SIGINT, so the terminal remains usable after Ctrl+C.

Purpose: When user hits Ctrl+C during readLineRaw (raw mode), RawModeGuard destructor does not run; handler restores termios so shell is not left in raw mode.
Output: g_repl_orig_termios, repl_sigint_handler, sigaction setup in runRepl.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/111-ergonomics-polish/111-RESEARCH.md
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: SIGINT handler with termios restore (ERGO-03)</name>
  <files>src/repl.h</files>
  <action>
Scope: `#if defined(__CYGWIN__) || !defined(_WIN32)` and `#ifndef __EMSCRIPTEN__`. Native Windows and Emscripten: no changes.

1. Add `#include <csignal>` in the existing `#if defined(__CYGWIN__) || !defined(_WIN32)` block (termios already included).

2. Add static `struct termios g_repl_orig_termios` and `void repl_sigint_handler(int)` before runRepl. Handler body (async-signal-safe only): `tcsetattr(STDIN_FILENO, TCSAFLUSH, &g_repl_orig_termios); signal(SIGINT, SIG_DFL); raise(SIGINT);` — no iostream, no malloc. Per 111-RESEARCH.md Pitfall 1.

3. In runRepl, inside the `if (stdin_is_tty())` block at the start (before loadHistory): if `tcgetattr(STDIN_FILENO, &g_repl_orig_termios) == 0`, install handler: `struct sigaction sa = {}; sa.sa_handler = repl_sigint_handler; sigemptyset(&sa.sa_mask); sa.sa_flags = 0; sigaction(SIGINT, &sa, &old);` — save old so we could restore on exit if desired; for this phase we do not restore (REPL exit is process exit). Guard the whole block with `#if defined(__CYGWIN__) || !defined(_WIN32)`.
</action>
  <verify>
grep -n "repl_sigint_handler\|g_repl_orig_termios\|sigaction" src/repl.h
Build: make -j (ensures POSIX code compiles on target)
Manual (CYGWIN): run qseries, type partial input, Ctrl+C — shell prompt should be usable (not raw mode).
</verify>
  <done>Ctrl+C during REPL raw mode restores terminal; shell accepts input normally after.</done>
</task>

</tasks>

<verification>
- make -j
- Manual: Ctrl+C during qseries input — terminal restored (run `reset` if uncertain)
</verification>

<success_criteria>
- ERGO-03: SIGINT handler restores termios, re-raises; terminal usable after Ctrl+C (POSIX/CYGWIN only)
</success_criteria>

<output>
After completion, create .planning/phases/111-ergonomics-polish/111-02-SUMMARY.md
</output>
