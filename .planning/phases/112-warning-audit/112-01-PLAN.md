---
phase: 112-warning-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/tcore.h, src/eta_cusp.h, Makefile]
autonomous: true

must_haves:
  truths:
    - "g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow produces zero warnings"
    - "tcore.h has no shadow or unused-parameter warnings"
    - "eta_cusp.h has no shadow or unused-parameter warnings"
    - "Makefile CXXFLAGS includes -Wshadow"
    - "All acceptance tests pass"
  artifacts:
    - path: src/tcore.h
      provides: t-core functions; no -Wshadow violations
    - path: src/eta_cusp.h
      provides: eta cusp / modular form proofs; no -Wshadow violations
    - path: Makefile
      provides: CXXFLAGS with -Wshadow
  key_links:
    - from: Makefile CXXFLAGS
      to: zero-warning build
      via: make all
      pattern: "-Wshadow"
---

<objective>
Eliminate all compiler warnings and add -Wshadow to the build. Fix tcore.h and eta_cusp.h (known warning sources per phase goal), then add -Wshadow to Makefile and resolve any newly exposed warnings across the codebase.
Purpose: HEALTH-01, HEALTH-02 — zero warnings, no global -Wno-* suppression.
Output: Clean build with -Wall -Wextra -Wpedantic -Wshadow; acceptance tests pass.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tcore.h
@src/eta_cusp.h
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix tcore.h warnings</name>
  <files>src/tcore.h</files>
  <action>
1. Fix unused parameter in tcore_tcores (line 149): parameter `n` is unused. Add `[[maybe_unused]]` to `n`: `tcore_tcores(int p, [[maybe_unused]] int n, ...)` per 112-RESEARCH.md.

2. Fix -Wshadow violations. Primary: tcore_tquot (lines 188–200): outer loop `for (int j = 0; j < total; ++j)` and inner loop `for (int64_t j = 0; j < nik; ++j)` — inner `j` shadows outer `j`. Rename inner variable to `jj`.

3. Search for other nested loops with shadow; fix by renaming inner variables. Do not use global -Wno-*.
  </action>
  <verify>make clean; make CXXFLAGS="-std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow" all 2>&1 | grep -c "warning:"; expect 0 for tcore-only changes. If other files warn, that is Task 3 scope.</verify>
  <done>tcore.h compiles with -Wshadow with zero warnings from this file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix eta_cusp.h warnings</name>
  <files>src/eta_cusp.h</files>
  <action>
Fix -Wshadow and -Wunused-parameter violations in eta_cusp.h.

provemodfuncGAMMA0UpETAid (lines 234–241) already uses commented parameter names (/*etaid*/, /*N*/, /*p*/) to avoid unused-parameter warnings. If the compiler still warns, use [[maybe_unused]] on the parameters or omit names only where appropriate — do not add global -Wno-unused-parameter.

Check for variable shadowing: loop variables or locals that shadow parameters or outer-scope variables. Rename inner variables to fix. No -Wno-* suppressions.

Verify: Build with -Wshadow does not emit warnings from eta_cusp.h.
  </action>
  <verify>Build with -Wall -Wextra -Wpedantic -Wshadow; grep output for eta_cusp — no warnings from this header.</verify>
  <done>eta_cusp.h compiles with -Wshadow with zero warnings from this file.</done>
</task>

<task type="auto">
  <name>Task 3: Add -Wshadow to Makefile and fix remaining warnings</name>
  <files>Makefile, src/*.h, src/*.cpp</files>
  <action>
1. Add -Wshadow to CXXFLAGS in Makefile. Change:
   CXXFLAGS = -std=c++20 -O2 -Wall -Wextra -Wpedantic
   to:
   CXXFLAGS = -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow

2. Run: make clean && make all 2>&1

3. Fix any remaining warnings across the codebase. Typical fixes:
   - Shadow: rename inner loop variable (e.g. j → jj) or local that shadows parameter
   - Unused parameter: omit name, use [[maybe_unused]], or (void)x — no global -Wno-unused-parameter
   - Do not add any -Wno-* flags globally

4. Rebuild until: make all 2>&1 produces zero warnings.

5. Run acceptance tests: make acceptance (or bash tests/acceptance.sh). All must pass.
  </action>
  <verify>make clean && make all 2>&1 | grep -E "warning:|error:" — no output. make acceptance (or ./tests/acceptance.sh) exits 0.</verify>
  <done>CXXFLAGS includes -Wshadow; full build is warning-free; acceptance tests pass.</done>
</task>

</tasks>

<verification>
- make all produces zero compiler warnings
- make acceptance (or tests/acceptance.sh) passes
- No -Wno-* in Makefile or build scripts
</verification>

<success_criteria>
1. g++ with -Wall -Wextra -Wpedantic -Wshadow compiles src/main.cpp with zero warnings
2. tcore.h and eta_cusp.h are warning-free
3. No global -Wno-* suppressions
4. All existing acceptance tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/112-warning-audit/112-01-SUMMARY.md`
</output>
