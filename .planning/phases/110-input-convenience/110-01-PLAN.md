---
phase: 110-input-convenience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/repl.h
  - tests/acceptance-input-convenience.sh
autonomous: true

must_haves:
  truths:
    - "Typing an undefined variable shows 'Did you mean: x?' when a close match exists in built-ins or user vars"
    - "Tab completion shows function signature hint when completing a built-in (single match)"
    - "Tab and history share single (line, pos) buffer — no cursor/history conflict"
  artifacts:
    - path: src/repl.h
      provides: formatUndefinedVariableMsg helper, sig hint in handleTabCompletion, single-buffer documentation
      contains: formatUndefinedVariableMsg, getHelpTable().find(match)
  key_links:
    - from: evalExpr Var case
      to: formatUndefinedVariableMsg
      via: throw std::runtime_error(formatUndefinedVariableMsg(...))
      pattern: formatUndefinedVariableMsg
    - from: handleTabCompletion
      to: getHelpTable
      via: sig lookup when matches.size()==1 and isFunc
      pattern: getHelpTable\(\).*sig
---

<objective>
Implement INPUT-01 (typo suggestions for undefined variables), INPUT-02 (signature hint on tab completion for built-ins), and INPUT-03 (document single-buffer design) per 110-RESEARCH.md.
Purpose: Users get helpful suggestions and completion hints without buffer conflicts.
Output: formatUndefinedVariableMsg helper, sig display in handleTabCompletion, single-buffer block comment, acceptance test.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-input-convenience/110-RESEARCH.md
@src/repl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: formatUndefinedVariableMsg + all throw sites (INPUT-01)</name>
  <files>src/repl.h</files>
  <action>
Add helper `formatUndefinedVariableMsg(const Environment& env, const std::string& name)` after `getCompletionCandidates` (before `handleTabCompletion`). Use `getCompletionCandidates(env)` as candidate source (NOT getHelpTable only — must suggest user vars too). Use `levenshteinDistance(name, key)` with threshold d<=3; up to 2 suggestions, sorted by distance. Return "undefined variable: " + name + ". Did you mean: A, B?" when suggestions exist; else "undefined variable: " + name.

Replace all 4 undefined-variable throw sites:
1. evalExpr Var case (~2266): `throw std::runtime_error(formatUndefinedVariableMsg(env, e->varName));`
2. jac2prod (~1038): `throw std::runtime_error(runtimeErr(name, formatUndefinedVariableMsg(env, args[0]->varName)));`
3. jac2series (~1052): same pattern as jac2prod
4. jac2eprod (~1506): `throw std::runtime_error(formatUndefinedVariableMsg(env, vname));`

Reference pattern: unknown built-in in dispatchBuiltin (2224–2242), but use getCompletionCandidates(env) not getHelpTable().
</action>
  <verify>
grep -n "formatUndefinedVariableMsg" src/repl.h | head -20
Run: printf 'rr := 1\nRR\n' | ./qseries 2>&1 | grep -q "Did you mean: rr"
</verify>
  <done>All 4 throw sites use formatUndefinedVariableMsg; undefined-var typo shows suggestions (built-in or user var).</done>
</task>

<task type="auto">
  <name>Task 2: Signature hint in handleTabCompletion (INPUT-02)</name>
  <files>src/repl.h</files>
  <action>
In handleTabCompletion, inside the `if (matches.size() == 1)` block, when `isFunc` is true:
1. Before updating line/pos: look up HelpEntry via getHelpTable().find(match)
2. If found and it->second.sig non-empty: print "\n  " + it->second.sig + "\n" to stdout
3. Then perform existing logic: match += "(", line update, pos = start + match.size(), redrawLineRaw

Only print sig when the single match is a built-in (getHelpTable().count(match) > 0). Do NOT print sig for user variables (no HelpEntry).
</action>
  <verify>
Grep handleTabCompletion in repl.h; confirm sig print branch exists when isFunc and matches.size()==1.
Manual: run qseries interactively, type "aqpro" and Tab — should see "  aqprod(a,q,n,T)" on next line, then completion.
</verify>
  <done>Single-match tab completion of built-in prints signature hint on next line; user vars complete without sig.</done>
</task>

<task type="auto">
  <name>Task 3: Document single-buffer design (INPUT-03)</name>
  <files>src/repl.h, tests/acceptance-input-convenience.sh</files>
  <action>
1. Add block comment immediately above readLineRaw (before line 2739): document that `line` and `pos` form the single editing buffer shared by tab completion and history navigation; no second buffer; handleTabCompletion and up/down arrows modify line/pos in place.

2. Create tests/acceptance-input-convenience.sh: run undefined-variable suggestion test (rr := 1; RR -> "Did you mean: rr") and optionally verify "unknown built-in" still gives suggestions (etamke -> "Did you mean: etamake"). Exit 0 if all pass.
</action>
  <verify>
grep -A2 "single.*buffer\|line.*pos.*buffer" src/repl.h
bash tests/acceptance-input-convenience.sh
</verify>
  <done>readLineRaw has documentation comment; acceptance test passes for undefined-var suggestions.</done>
</task>

</tasks>

<verification>
- Run tests/acceptance-input-convenience.sh
- Manual: tab-complete a built-in (e.g. aqpro + Tab) — sig hint appears
- Build: make -j
</verification>

<success_criteria>
- INPUT-01: Undefined variable error shows "Did you mean: X?" when Levenshtein d<=3
- INPUT-02: Tab on single built-in match shows HelpEntry.sig on next line
- INPUT-03: Block comment documents single (line, pos) buffer; no second buffer in code
</success_criteria>

<output>
After completion, create .planning/phases/110-input-convenience/110-01-SUMMARY.md
</output>
