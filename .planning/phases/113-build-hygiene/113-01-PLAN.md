---
phase: 113-build-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [build.sh, .github/workflows/release.yml]
autonomous: true

must_haves:
  truths:
    - "build.sh uses same warning flags as Makefile (-Wall -Wextra -Wpedantic -Wshadow)"
    - "release.yml uses same warning flags as Makefile (-Wall -Wextra -Wpedantic -Wshadow)"
  artifacts:
    - path: build.sh
      provides: "CXXFLAGS with -Wpedantic -Wshadow in all g++ invocations"
      contains: "-Wpedantic -Wshadow"
    - path: .github/workflows/release.yml
      provides: "CXXFLAGS with -Wpedantic -Wshadow in Build step"
      contains: "-Wpedantic -Wshadow"
  key_links:
    - from: build.sh
      to: Makefile
      via: "CXXFLAGS alignment"
      pattern: "-Wall -Wextra -Wpedantic -Wshadow"
    - from: .github/workflows/release.yml
      to: Makefile
      via: "CXXFLAGS alignment"
      pattern: "-Wall -Wextra -Wpedantic -Wshadow"
---

<objective>
Align CXXFLAGS across build.sh and release.yml with Makefile. Add -Wpedantic -Wshadow to both; Makefile already has them (Phase 112).
Purpose: Zero warnings from make, build.sh, and CI (HEALTH-03).
Output: build.sh and release.yml with consistent -Wall -Wextra -Wpedantic -Wshadow.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-build-hygiene/113-RESEARCH.md
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add -Wpedantic -Wshadow to build.sh</name>
  <files>build.sh</files>
  <action>
Add -Wpedantic -Wshadow to all three g++ invocations in build.sh, in order -Wall -Wextra -Wpedantic -Wshadow to match Makefile:

1. Line 6: mingw branch — change `-Wall -Wextra` to `-Wall -Wextra -Wpedantic -Wshadow`
2. Line 8: g++ with -static — same change
3. Line 9: g++ fallback (no -static) — same change

Reference: Makefile line 4: CXXFLAGS = -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow
  </action>
  <verify>grep -c '\-Wpedantic' build.sh returns 3</verify>
  <done>All three g++ invocations in build.sh include -Wpedantic -Wshadow; build.sh produces zero warnings for clean build</done>
</task>

<task type="auto">
  <name>Task 2: Add -Wpedantic -Wshadow to release.yml</name>
  <files>.github/workflows/release.yml</files>
  <action>
Add -Wpedantic -Wshadow to the Build step g++ command (line 29). Place before ${{ matrix.static }} so flags apply on both linux and macos matrix runs.

Change:
  g++ -std=c++20 -O2 -Wall -Wextra ${{ matrix.static }} -o qseries src/main.cpp
To:
  g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wshadow ${{ matrix.static }} -o qseries src/main.cpp
  </action>
  <verify>grep '\-Wpedantic -Wshadow' .github/workflows/release.yml</verify>
  <done>release.yml Build step includes -Wpedantic -Wshadow; CI produces zero warnings</done>
</task>

</tasks>

<verification>
- Run: bash build.sh (local) — no warnings
- Run: make clean && make (local) — no warnings  
- Push tag: CI Build step — no warnings
</verification>

<success_criteria>
- build.sh: all 3 g++ invocations have -Wall -Wextra -Wpedantic -Wshadow
- release.yml: Build step has -Wall -Wextra -Wpedantic -Wshadow
- make, build.sh, and CI produce zero compiler warnings
</success_criteria>

<output>
After completion, create `.planning/phases/113-build-hygiene/113-01-SUMMARY.md`
</output>
