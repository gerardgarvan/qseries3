---
phase: 59-install-script
plan: 01
type: execute
wave: 1
depends_on: [57]
files_modified:
  - install.sh
autonomous: true

must_haves:
  truths:
    - "curl -fsSL <url> | sh installs qseries to ~/.local/bin"
    - "Platform detection via uname for Linux/macOS"
    - "SHA256 checksum verification before install"
    - "--version flag for specific version selection"
    - "Script wrapped in main() for partial-download protection"
  artifacts:
    - path: "install.sh"
      provides: "One-liner install script"
---

<objective>
Create install.sh — a POSIX shell install script that:
1. Detects platform (Linux x86_64, macOS arm64/x86_64)
2. Downloads binary from GitHub Releases
3. Verifies SHA256 checksum
4. Installs to ~/.local/bin
5. Supports --version flag

Output: `install.sh`
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create install.sh</name>
  <files>install.sh</files>
  <action>
POSIX sh script with:
- set -eu
- main() wrapper for partial-download protection
- Platform detection: uname -s (Linux/Darwin) + uname -m (x86_64/arm64)
- Map to binary names: qseries-linux-amd64, qseries-darwin-arm64
- Download from GitHub Releases (curl or wget fallback)
- Download checksums, verify with sha256sum/shasum
- Install to ~/.local/bin, chmod +x
- --version flag for pinning, default to latest
- Print PATH instructions if ~/.local/bin not on PATH
  </action>
  <done>install.sh created</done>
</task>

</tasks>
