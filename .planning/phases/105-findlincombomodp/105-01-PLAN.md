---
phase: 105-findlincombomodp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "User can call findlincombomodp(f, L, p, T) from REPL"
    - "findlincombomodp returns coefficients in F_p such that f ≡ Σ c_i L_i (mod p)"
    - "REPL help(findlincombomodp) documents usage"
    - "acceptance-modp.sh findlincombomodp tests (Test 7, Test 8) pass"
  artifacts:
    - path: src/relations.h
      provides: findlincombomodp implementation
      contains: "findlincombomodp"
    - path: src/repl.h
      provides: REPL dispatch and help for findlincombomodp
      contains: "findlincombomodp"
    - path: tests/acceptance-modp.sh
      provides: findlincombomodp tests (Test 7, Test 8)
      contains: "findlincombomodp"
  key_links:
    - from: src/repl.h
      to: src/relations.h
      via: findlincombomodp dispatch
      pattern: "findlincombomodp"
    - from: tests/acceptance-modp.sh
      to: findlincombomodp
      via: Test 7 (f=L1+L2) and Test 8 (no-error run)
      pattern: "findlincombomodp"
---

<objective>
Verification-first phase: run acceptance-modp.sh to confirm findlincombomodp exists, REPL works, and tests pass. Phase 99 already implemented findlincombomodp (relations.h, linalg.h solve_modp, repl.h dispatch, acceptance-modp.sh). If all pass, phase is verification-only; if any findlincombomodp test fails, fix regression.

Purpose: Close GAP11-03; confirm findlincombomodp is verified and operational
Output: 105-01-SUMMARY.md (verification record; code edits only if regressions found)
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-findlincombomodp/99-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run acceptance-modp; verify findlincombomodp tests pass</name>
  <files></files>
  <action>
1. Build: `make` or `g++ -std=c++20 -O2 -o qseries main.cpp` (ensure qseries or qseries.exe exists in project root)
2. Run: `bash tests/acceptance-modp.sh`
3. Check output: Tests 7 and 8 must pass:
   - Test 7 "findlincombomodp f=L1+L2": findlincombomodp(etaq(1,30)+etaq(2,30), [etaq(1,30), etaq(2,30)], 7) → output matches "1 L1.*1 L2.*mod 7"
   - Test 8 "findlincombomodp runs": findlincombomodp(etaq(1,20), [etaq(1,20)], 5) runs without error
4. If Tests 7 and 8 pass: phase is complete (verification-only). Skip Task 2. Create 105-01-SUMMARY.md with status "verification-only, no code changes".
5. If either Test 7 or 8 fails: proceed to Task 2.
  </action>
  <verify>
bash tests/acceptance-modp.sh 2>&1 | grep -E "findlincombomodp|PASS|FAIL|Test 7|Test 8"
  </verify>
  <done>
findlincombomodp Test 7 and Test 8 show PASS. Or failures documented for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix regression if findlincombomodp tests failed</name>
  <files>src/relations.h, src/linalg.h, src/repl.h</files>
  <action>
Execute ONLY if Task 1 found Test 7 or Test 8 failures. Do NOT re-implement findlincombomodp from scratch — Phase 99 work must be reused.

1. **Diagnose**: Check relations.h findlincombomodp, linalg.h solve_modp, repl.h dispatch. Verify modp/b matrix construction; check output format matches "1 L1.*1 L2.*mod 7".
2. **Fix**: Apply minimal fix — e.g. correct coefficient display, solve_modp return handling, or REPL formatFindlincombomodp output format.
3. **Verify**: Re-run `bash tests/acceptance-modp.sh`. Tests 7 and 8 must pass.
4. **Regression**: Run `make acceptance-maple` (or equivalent) to confirm no regressions.
  </action>
  <verify>
bash tests/acceptance-modp.sh 2>&1
make acceptance-maple 2>&1 || true
  </verify>
  <done>
findlincombomodp Test 7 and Test 8 pass; no regressions.
  </done>
</task>

</tasks>

<verification>
1. acceptance-modp.sh Test 7 (findlincombomodp f=L1+L2): PASS
2. acceptance-modp.sh Test 8 (findlincombomodp runs): PASS
3. If fix applied: acceptance-maple shows no new failures
</verification>

<success_criteria>
- findlincombomodp(f, L, p, T) callable from REPL
- acceptance-modp.sh findlincombomodp tests pass
- GAP11-03 satisfied: mod-p linear combination with REPL dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/105-findlincombomodp/105-01-SUMMARY.md`
</output>
