---
phase: 90-symbolic-z-tripleprod-quinprod
plan: 02
type: execute
wave: 2
depends_on: [90-01]
files_modified: [src/repl.h, tests/maple-checklist.sh]
autonomous: true

must_haves:
  truths:
    - "tripleprod(z,q,T) with symbolic z returns bivariate series and displays it"
    - "quinprod(z,q,T) with symbolic z returns bivariate series and displays it"
    - "Maple checklist Block 28 passes (tripleprod(z,q,10))"
    - "Maple checklist Block 32 passes (quinprod(z,q,3))"
  artifacts:
    - path: src/repl.h
      provides: symbolic z detection and bivariate display
      contains: "tripleprod_symbolic|quinprod_symbolic|formatBivariate"
  key_links:
    - from: tripleprod dispatch
      to: tripleprod_symbolic
      via: symbolic z branch
      pattern: "tripleprod_symbolic"
    - from: quinprod dispatch
      to: quinprod_symbolic
      via: symbolic z branch
      pattern: "quinprod_symbolic"
---

<objective>
Wire symbolic z detection into tripleprod and quinprod REPL dispatch, add BivariateSeries to EvalResult and display, and update maple-checklist Blocks 28 and 32 to pass.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-symbolic-z-tripleprod-quinprod/90-CONTEXT.md
@.planning/phases/90-symbolic-z-tripleprod-quinprod/90-01-SUMMARY.md
@src/repl.h
@src/qfuncs.h
@tests/maple-checklist.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symbolic z detection and dispatch for tripleprod/quinprod</name>
  <files>src/repl.h</files>
  <action>
Add symbolic z detection and symbolic-path dispatch to tripleprod and quinprod in dispatchBuiltin.

**Symbolic z detection:** args[0]->tag == Expr::Tag::Var AND (env.env.find(varName) == env.env.end() OR varName == "z").
Per CONTEXT: "var not in env OR name==\"z\"". Use: not in env, or explicitly "z" when in env (optional convention). Simplest: Var and not in env → symbolic.

**tripleprod branch** (before existing ev(0) call):
1. If args[0]->tag == Var and env.env.find(args[0]->varName) == env.env.end():
   - Get q from args[1]: Series q = ev(1)
   - int Tr = evi(2)
   - auto b = tripleprod_symbolic(q, Tr)
   - std::cout << formatBivariate(b) << std::endl
   - return DisplayOnly{}  (or add BivariateSeries to EvalResult and return it)
2. Otherwise fall through to existing: return tripleprod(ev(0), ev(1), evi(2))

**quinprod branch** (after prodid/seriesid Var check on args[2], before ev(0)):
1. Ensure args[2] is integer T (not prodid/seriesid) — existing code already handles prodid/seriesid first.
2. If args[0]->tag == Var and env.env.find(args[0]->varName) == env.env.end():
   - Series q = ev(1); int Tr = evi(2)
   - auto b = quinprod_symbolic(q, Tr)
   - std::cout << formatBivariate(b) << std::endl
   - return DisplayOnly{}
3. Otherwise: return quinprod(ev(0), ev(1), evi(2))

Include qfuncs.h for tripleprod_symbolic, quinprod_symbolic, formatBivariate (already included). Add forward declarations if needed.

**Alternative:** Return BivariateSeries in EvalResult for consistency with other types. Add BivariateSeries to EvalResult variant and to display(). Then assignable: tp := tripleprod(z,q,10). For Phase 90, DisplayOnly is sufficient; assignment can be deferred.
</action>
  <verify>echo "tripleprod(z,q,10)" | ./dist/qseries.exe 2>&1 | head -5</verify>
  <done>tripleprod(z,q,10) and quinprod(z,q,3) with undefined z produce bivariate output instead of error</done>
</task>

<task type="auto">
  <name>Task 2: Maple checklist Blocks 28 and 32 pass</name>
  <files>tests/maple-checklist.sh</files>
  <action>
Replace skip with real tests for Blocks 28 and 32 in tests/maple-checklist.sh.

**Block 28** (currently skip):
```bash
# Block 28: tripleprod(z,q,10) — symbolic z
if run "tripleprod(z,q,10)" | grep -qE 'z|q'; then
    pass "Block 28: tripleprod symbolic z"
else
    fail "Block 28: tripleprod symbolic z"
fi
```
Require output to contain 'z' or 'q' (bivariate display). A stricter check: grep for a Laurent term pattern if format is known.

**Block 32** (currently skip):
```bash
# Block 32: quinprod(z,q,3) — symbolic z
if run "quinprod(z,q,3)" | grep -qE 'z|q'; then
    pass "Block 32: quinprod symbolic z"
else
    fail "Block 32: quinprod symbolic z"
fi
```

Ensure z is undefined when running (no prior z := ...). The run helper typically starts fresh env per block, but Blocks 28 and 32 run in sequence — confirm no z assignment in earlier blocks that would leak.
</action>
  <verify>./tests/maple-checklist.sh 2>&1 | grep -E "Block 28|Block 32"</verify>
  <done>Block 28 and Block 32 show "pass" in maple-checklist.sh output</done>
</task>

</tasks>

<verification>
- maple-checklist.sh: Blocks 28 and 32 pass
- tripleprod(q,q^3,100) still works (Block 29 — no regression)
- quinprod(q,q^5,20) still works (Block 36 — no regression)
- quinprod(z,q,prodid) and quinprod(z,q,seriesid) unchanged (Blocks 30-31)
</verification>

<success_criteria>
- GAP-SYM-01: tripleprod(z,q,T) with symbolic z returns bivariate series
- GAP-SYM-02: quinprod(z,q,T) with symbolic z returns bivariate series
- Maple checklist Blocks 28 and 32 pass
</success_criteria>

<output>
After completion, create `.planning/phases/90-symbolic-z-tripleprod-quinprod/90-02-SUMMARY.md`
</output>
