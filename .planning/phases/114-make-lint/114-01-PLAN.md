---
phase: 114-make-lint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Makefile]
autonomous: true

must_haves:
  truths:
    - "make lint runs cppcheck"
    - "cppcheck uses --enable=warning,style,performance"
    - "make lint completes and reports (no compile DB)"
  artifacts:
    - path: Makefile
      provides: lint target
      contains: "lint:"
  key_links:
    - from: Makefile
      to: src/main.cpp
      via: cppcheck invocation
      pattern: "cppcheck.*src/main.cpp"
---

<objective>
Add `make lint` target that runs cppcheck for static analysis.

Purpose: Provide static analysis via cppcheck without requiring compile_commands.json.
Output: Makefile with lint target; `make lint` runs cppcheck and reports findings.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-make-lint/114-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add make lint target to Makefile</name>
  <files>Makefile</files>
  <action>
1. Add `lint` to the .PHONY list in Makefile.
2. Add lint target that invokes:
   cppcheck --enable=warning,style,performance -I src src/main.cpp
   Per 114-RESEARCH.md: single-TU project; main.cpp includes all headers; -I src resolves includes; no compile_commands.json.
3. Use a tab-indented recipe. Do not add --error-exitcode=1 (phase says "completes and reports").
  </action>
  <verify>make lint; exits 0 and prints cppcheck output (warnings/notes or "Checking done").</verify>
  <done>make lint runs cppcheck with --enable=warning,style,performance on src/main.cpp and reports findings or passes.</done>
</task>

</tasks>

<verification>
- `make lint` invokes cppcheck
- cppcheck uses --enable=warning,style,performance -I src src/main.cpp
- No compile_commands.json or other setup required
- make lint completes (exit 0)
</verification>

<success_criteria>
1. make lint runs cppcheck
2. --enable=warning,style,performance used
3. No compile database required
4. Completes and reports
</success_criteria>

<output>
After completion, create `.planning/phases/114-make-lint/114-01-SUMMARY.md`
</output>
