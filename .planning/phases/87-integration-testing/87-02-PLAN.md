---
phase: 87-integration-testing
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - tests/integration-eta-theta-modforms.sh
  - Makefile
autonomous: true

must_haves:
  truths:
    - "Cross-package chain ETA + theta IDs + modforms runs end-to-end"
    - "DELTA12 and provemodfuncGAMMA0id used together in one test"
    - "RR series → jacprodmake → jac2eprod → provemodfuncid chain verified"
  artifacts:
    - path: tests/integration-eta-theta-modforms.sh
      provides: Cross-package integration tests
      min_lines: 60
  key_links:
    - from: tests/integration-eta-theta-modforms.sh
      to: eta_cusp.h, modforms.h, theta_ids.h
      via: provemodfuncGAMMA0id, DELTA12, jac2eprod, provemodfuncid
      pattern: "provemodfuncGAMMA0id|DELTA12|jac2eprod|provemodfuncid"
---

<objective>
Cross-package integration: Create `tests/integration-eta-theta-modforms.sh` that verifies end-to-end flows chaining ETA identity prover, modular forms basis, and theta IDs (jac2eprod, provemodfuncid).

Purpose: INT-02 — cross-package integration: ETA + thetaids + modforms identity proofs work end-to-end.

Output: New integration script with 2–3 tests; Makefile target; script included in full regression.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/87-integration-testing/87-RESEARCH.md
@.planning/phases/87-integration-testing/87-CONTEXT.md

Cross-package scenarios from 87-RESEARCH.md:
- Scenario 1: DELTA12 = q*eta^24; provemodfuncGAMMA0id proves eta identity (ETA + modforms)
- Scenario 2: rr → jacprodmake → jac2eprod → provemodfuncid (theta IDs chain)
- Pattern: use BIN fallback like acceptance-eta-cusp.sh; check() helper with grep -qE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration-eta-theta-modforms.sh</name>
  <files>tests/integration-eta-theta-modforms.sh</files>
  <action>
Create `tests/integration-eta-theta-modforms.sh` with:

1. **BIN resolution** (same as acceptance-eta-cusp.sh): dist/qseries.exe → dist/qseries → qseries.exe → qseries, error if none found.

2. **check() helper**: `check "desc" "input" "pattern"` — pipe input to BIN, grep -qE pattern, PASS/FAIL.

3. **Test 1 — ETA + modforms chain:** 
   - DELTA12(50) - q*etaq(1,50)^24 = 0 (modforms and qfuncs produce identical Delta)
   - provemodfuncGAMMA0id([[1, 2,24, 1,-24], [-1, 2,24, 1,-24]], 2) yields "proven=1" (eta-quotient identity on Gamma_0(2))

4. **Test 2 — Theta IDs chain:**
   - Multi-line: rr := sum(q^(n^2)/aqprod(q,q,n,50), n, 0, 8); j := jacprodmake(rr, 50); jac2eprod(j) — output must contain GETA or EETA
   - provemodfuncid([[1, [5,25,-1], [10,25,1]], [-1, [5,25,-1], [10,25,1]]], 25) yields "proven=1"

5. **Test 3 — ETA roundtrip (optional but valuable):**
   - etamake(theta3, 100) yields eta product; provemodfuncGAMMA0id on that form minus itself = 0, or a simpler check that etamake produces valid [[...]] output.

Use printf '%s\n' for multi-line input. Report "Total: N passed, M failed" and exit $FAIL.
</action>
  <verify>Run `bash tests/integration-eta-theta-modforms.sh` from repo root; exit 0 with all tests passing.</verify>
  <done>integration-eta-theta-modforms.sh exists; at least 2 cross-package tests pass; ETA + modforms + theta IDs chain verified.</done>
</task>

<task type="auto">
  <name>Task 2: Add Makefile target and wire into acceptance-all</name>
  <files>Makefile</files>
  <action>
1. Add `acceptance-integration-eta-theta` target (or `integration-eta-theta-modforms`) that runs `bash tests/integration-eta-theta-modforms.sh`.

2. Add this target to the `acceptance-all` dependency sequence (from 87-01). Order: maple-checklist, run-all, acceptance-modforms, acceptance-theta-ids, integration-eta-theta-modforms. Ensure acceptance-all runs all five stages.
</action>
  <verify>`make acceptance-integration-eta-theta` runs the new script; `make acceptance-all` includes it and completes successfully.</verify>
  <done>Makefile has integration target; acceptance-all runs the cross-package integration script.</done>
</task>

</tasks>

<verification>
- `bash tests/integration-eta-theta-modforms.sh` exits 0
- `make acceptance-all` runs integration-eta-theta-modforms.sh as part of full regression
- Cross-package flows (DELTA12, provemodfuncGAMMA0id, jacprodmake→jac2eprod→provemodfuncid) are exercised
</verification>

<success_criteria>
1. integration-eta-theta-modforms.sh exists with BIN resolution and 2+ cross-package tests
2. ETA identity prover + modular forms basis used together (Test 1)
3. Theta IDs chain (jac2eprod, provemodfuncid) verified (Test 2)
4. Script is part of make acceptance-all
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-testing/87-02-SUMMARY.md`
</output>
