---
phase: 87-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - tests/integration-tcore.sh
  - tests/acceptance-bailey.sh
autonomous: true

must_haves:
  truths:
    - "make acceptance-all runs full regression (maple-checklist, run-all, acceptance-modforms, acceptance-theta-ids)"
    - "All test scripts find the binary when it is at dist/qseries.exe"
    - "integration-tcore.sh and acceptance-bailey.sh use BIN fallback chain"
  artifacts:
    - path: Makefile
      provides: acceptance-all target
      contains: "acceptance-all"
    - path: tests/integration-tcore.sh
      provides: QSERIES/BIN with dist/ fallback
      contains: "dist/qseries"
    - path: tests/acceptance-bailey.sh
      provides: BIN with dist/ fallback
      contains: "dist/qseries"
  key_links:
    - from: Makefile
      to: tests/maple-checklist.sh
      via: acceptance-all target
      pattern: "acceptance-maple|maple-checklist"
    - from: Makefile
      to: tests/run-all.sh
      via: acceptance-all target
      pattern: "run-all"
---

<objective>
Full regression orchestration: Add `acceptance-all` Makefile target that runs maple-checklist, run-all, acceptance-modforms, and acceptance-theta-ids. Fix BIN/QSERIES resolution in integration-tcore.sh and acceptance-bailey.sh so they find the binary when it lives at `dist/qseries.exe`.

Purpose: INT-01 — all existing tests pass; close the gap where run-all omits maple-checklist, acceptance-modforms, acceptance-theta-ids; ensure scripts work when binary is only in dist/.

Output: Makefile with acceptance-all target; integration-tcore.sh and acceptance-bailey.sh with portable BIN resolution.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-integration-testing/87-RESEARCH.md
@.planning/phases/87-integration-testing/87-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add acceptance-all target and fix BIN in integration-tcore.sh</name>
  <files>Makefile, tests/integration-tcore.sh</files>
  <action>
1. **Makefile:** Add `acceptance-all` target that depends on `dist/qseries.exe` and runs in sequence:
   - `$(MAKE) acceptance-maple`
   - `bash tests/run-all.sh`
   - `$(MAKE) acceptance-modforms`
   - `$(MAKE) acceptance-theta-ids`
   Add `acceptance-all` to .PHONY.

2. **tests/integration-tcore.sh:** Replace hardcoded `QSERIES="./qseries --no-banner"` with BIN resolution:
   - Use the same fallback chain as acceptance-eta-cusp.sh: try `./dist/qseries.exe`, `./dist/qseries`, `./qseries.exe`, `./qseries`; error if none found.
   - Set `QSERIES="$BIN --no-banner"` after resolution.
   - Script must cd to repo root before resolving (run-all.sh does `cd "$(dirname "$0")/.."` before invoking scripts; integration-tcore is invoked as `bash tests/integration-tcore.sh` so cwd is repo root).
</action>
  <verify>cd to repo root, run `make acceptance-maple` and `bash tests/run-all.sh`; both complete with exit 0. Then run `make acceptance-all` — all four stages run.</verify>
  <done>acceptance-all target exists; integration-tcore.sh uses BIN fallback; make acceptance-all invokes all four stages.</done>
</task>

<task type="auto">
  <name>Task 2: Fix BIN resolution in acceptance-bailey.sh</name>
  <files>tests/acceptance-bailey.sh</files>
  <action>
Replace hardcoded `./qseries.exe` with BIN resolution matching acceptance-eta-cusp.sh:
- Add at top (after shebang and PASS/FAIL init):
  `BIN="./dist/qseries.exe"`
  `[ -f "$BIN" ] || BIN="./dist/qseries"`
  `[ -f "$BIN" ] || BIN="./qseries.exe"`
  `[ -f "$BIN" ] || BIN="./qseries"`
  `[ -f "$BIN" ] || { echo "error: qseries binary not found (run make first)"; exit 1; }`
- In the check() function, replace `./qseries.exe` with `"$BIN"`.
</action>
  <verify>Run `bash tests/acceptance-bailey.sh` from repo root with binary at dist/qseries.exe; script exits 0 with "Total: N passed, 0 failed".</verify>
  <done>acceptance-bailey.sh uses BIN fallback chain; passes when binary is in dist/.</done>
</task>

</tasks>

<verification>
- `make acceptance-all` runs without error and reports totals from each stage
- `make acceptance-maple` passes (or exits with known skip count)
- `bash tests/run-all.sh` passes (includes integration-tcore and acceptance-bailey)
- `make acceptance-modforms` and `make acceptance-theta-ids` pass
</verification>

<success_criteria>
1. `make acceptance-all` exists and runs maple-checklist + run-all + acceptance-modforms + acceptance-theta-ids
2. integration-tcore.sh and acceptance-bailey.sh resolve binary via dist/ fallback when binary is at dist/qseries.exe
3. Full regression can be invoked with a single command
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-testing/87-01-SUMMARY.md`
</output>
