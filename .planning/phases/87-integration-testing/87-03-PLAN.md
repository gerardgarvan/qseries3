---
phase: 87-integration-testing
plan: 03
type: execute
wave: 3
depends_on: [87-01, 87-02]
gap_closure: true
files_modified:
  - tests/acceptance-rank-crank.sh
  - tests/acceptance-crank.sh
  - tests/acceptance-mock.sh
  - tests/acceptance-ptnstats.sh
  - tests/acceptance-tcore.sh
  - tests/acceptance-gsk.sh
  - tests/acceptance-vectors.sh
  - tests/acceptance-tcrank-display.sh
  - tests/acceptance-partition-type.sh
autonomous: true

must_haves:
  truths:
    - "All test scripts find the binary when it is at dist/qseries.exe"
  artifacts:
    - path: tests/acceptance-rank-crank.sh
      provides: BIN fallback for rank/crank GF tests
      contains: "dist/qseries"
    - path: tests/acceptance-crank.sh
      provides: BIN fallback for crank table tests
      contains: "dist/qseries"
    - path: tests/acceptance-mock.sh
      provides: BIN fallback for mock theta tests
      contains: "dist/qseries"
    - path: tests/acceptance-ptnstats.sh
      provides: BIN fallback for partition stats tests
      contains: "dist/qseries"
    - path: tests/acceptance-tcore.sh
      provides: BIN fallback for t-core tests
      contains: "dist/qseries"
    - path: tests/acceptance-gsk.sh
      provides: BIN fallback for GSK tests
      contains: "dist/qseries"
    - path: tests/acceptance-vectors.sh
      provides: BIN fallback for vector tests
      contains: "dist/qseries"
    - path: tests/acceptance-tcrank-display.sh
      provides: BIN fallback for tcrank display tests
      contains: "dist/qseries"
    - path: tests/acceptance-partition-type.sh
      provides: BIN fallback for partition type tests
      contains: "dist/qseries"
  key_links:
    - from: tests/run-all.sh
      to: "all 9 scripts"
      via: bash invocation
      pattern: "acceptance-rank-crank|acceptance-crank|acceptance-mock|acceptance-ptnstats|acceptance-tcore|acceptance-gsk|acceptance-vectors|acceptance-tcrank-display|acceptance-partition-type"
---

<objective>
Close the BIN fallback gap: Add the same BIN resolution chain used in acceptance-bailey.sh to 9 run-all.sh scripts that currently use hardcoded ./qseries or ./qseries.exe.

Purpose: When the binary is built only to dist/qseries.exe (Makefile default), these scripts fail because they look for ./qseries or ./qseries.exe in the repo root.

Output: All 9 scripts resolve binary via dist/qseries.exe → dist/qseries → qseries.exe → qseries fallback chain.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-integration-testing/87-VERIFICATION.md
@tests/acceptance-bailey.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BIN fallback to rank/crank scripts (check pattern)</name>
  <files>tests/acceptance-rank-crank.sh, tests/acceptance-crank.sh</files>
  <action>
Both scripts use a check() function that pipes to `./qseries.exe`. Add the BIN fallback block and replace the hardcoded binary.

**Reference pattern (from acceptance-bailey.sh lines 4-8):**
```bash
BIN="./dist/qseries.exe"
[ -f "$BIN" ] || BIN="./dist/qseries"
[ -f "$BIN" ] || BIN="./qseries.exe"
[ -f "$BIN" ] || BIN="./qseries"
[ -f "$BIN" ] || { echo "error: qseries binary not found (run make first)"; exit 1; }
```

For each script:
1. Insert the BIN block immediately after the shebang and PASS=0; FAIL=0 line, before the check() definition.
2. In the check() function, replace `./qseries.exe` with `"$BIN"` in the pipe (e.g. `out=$(echo "$input" | "$BIN" 2>&1)`).

Scripts: acceptance-rank-crank.sh, acceptance-crank.sh
</action>
  <verify>From repo root with binary only at dist/qseries.exe: run `bash tests/acceptance-rank-crank.sh` and `bash tests/acceptance-crank.sh`; both exit 0 with "Total: N passed, 0 failed".</verify>
  <done>Both rank-crank and crank scripts use BIN fallback; pass when binary is in dist/.</done>
</task>

<task type="auto">
  <name>Task 2: Add BIN fallback to QSERIES scripts (7 scripts)</name>
  <files>tests/acceptance-mock.sh, tests/acceptance-ptnstats.sh, tests/acceptance-tcore.sh, tests/acceptance-gsk.sh, tests/acceptance-vectors.sh, tests/acceptance-tcrank-display.sh, tests/acceptance-partition-type.sh</files>
  <action>
All 7 scripts use `QSERIES="./qseries --no-banner"` (or `QSERIES="./qseries --no-banner"`) at the top. Replace with BIN resolution plus QSERIES.

**Reference pattern (from acceptance-bailey.sh + integration-tcore.sh):**
```bash
BIN="./dist/qseries.exe"
[ -f "$BIN" ] || BIN="./dist/qseries"
[ -f "$BIN" ] || BIN="./qseries.exe"
[ -f "$BIN" ] || BIN="./qseries"
[ -f "$BIN" ] || { echo "error: qseries binary not found (run make first)"; exit 1; }
QSERIES="$BIN --no-banner"
```

For each script:
1. Replace the line `QSERIES="./qseries --no-banner"` with the full BIN block (5 lines) followed by `QSERIES="$BIN --no-banner"`.
2. Preserve the shebang and any comment lines; place the BIN block after shebang and before PASS=0; FAIL=0.

Scripts: acceptance-mock.sh, acceptance-ptnstats.sh, acceptance-tcore.sh, acceptance-gsk.sh, acceptance-vectors.sh, acceptance-tcrank-display.sh, acceptance-partition-type.sh
</action>
  <verify>From repo root with binary only at dist/qseries.exe: run each of the 7 scripts; all exit 0 with "Total: N passed, 0 failed" (or equivalent pass line). Then run `bash tests/run-all.sh`; completes with 0 total failures.</verify>
  <done>All 7 QSERIES scripts use BIN fallback; run-all.sh passes when binary is in dist/.</done>
</task>

</tasks>

<verification>
- Each of the 9 scripts runs successfully with binary at dist/qseries.exe only
- `bash tests/run-all.sh` completes with 0 total failures when binary is at dist/qseries.exe
- No hardcoded ./qseries or ./qseries.exe remains in the 9 scripts
</verification>

<success_criteria>
1. All 9 run-all scripts resolve binary via BIN fallback chain (dist/qseries.exe → dist/qseries → qseries.exe → qseries)
2. Gap truth "All test scripts find the binary when it is at dist/qseries.exe" is satisfied
3. make acceptance-all can complete when binary is built to dist/qseries.exe only
</success_criteria>

<output>
After completion, create `.planning/phases/87-integration-testing/87-03-SUMMARY.md`
</output>
