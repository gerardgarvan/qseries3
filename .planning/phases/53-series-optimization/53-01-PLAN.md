---
phase: 53-series-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/series.h
autonomous: true

must_haves:
  truths:
    - "Series multiplication inner loop uses break instead of continue when exp >= trunc"
    - "All existing acceptance tests still pass"
    - "Rogers-Ramanujan prodmake still produces correct output"
  artifacts:
    - path: "src/series.h"
      provides: "Optimized series multiplication"
      contains: "break"
  key_links:
    - from: "Series::operator*"
      to: "inner loop break"
      via: "std::map ordered iteration guarantees all subsequent e2 values exceed trunc"
      pattern: "early-termination"
---

<objective>
Change one word in Series::operator* — `continue` to `break` on line 141 of series.h. Since std::map iterates in ascending key order, once `e1 + e2 >= trunc`, all subsequent `e2` values will also exceed trunc (they're larger). Breaking out of the inner loop instead of continuing to check every remaining pair gives 2-4x speedup.

Purpose: Performance — faster series multiplication via inner-loop early termination.
Output: Modified `src/series.h` with one-word change.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change continue to break in Series::operator*</name>
  <files>src/series.h</files>
  <action>
In `Series::operator*` (line ~140 of series.h), change:

```cpp
if (exp >= t)  // skip early (PITFALLS: mult trunc skip)
    continue;
```

to:

```cpp
if (exp >= t)
    break;
```

**Why this works:** `std::map<int, Frac>` iterates in ascending key order. The inner loop iterates over `o.c` (the second series's coefficients) in order of increasing exponent `e2`. Once `e1 + e2 >= t`, every subsequent `e2'` satisfies `e2' > e2`, so `e1 + e2' > e1 + e2 >= t` — all remaining terms exceed truncation. Breaking immediately skips all of them.

**Build and test:**
- Build with `g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -static -o qseries src/main.cpp`
- Run all acceptance tests
- Verify Rogers-Ramanujan prodmake produces correct output
  </action>
  <verify>
1. Builds without warnings
2. All acceptance tests pass
3. Rogers-Ramanujan prodmake produces correct output
4. The change is exactly one word: continue → break
  </verify>
  <done>Series multiplication inner-loop early break implemented</done>
</task>

</tasks>

<verification>
1. `break` replaces `continue` in inner loop of Series::operator*
2. All existing acceptance tests pass
3. Rogers-Ramanujan prodmake still produces correct output
4. Build produces zero warnings
</verification>

<success_criteria>
- Series multiplication skips inner-loop iterations where exponent sum exceeds truncation
- Correctness preserved — break is safe because map iteration is ordered
</success_criteria>

<output>
After completion, create `.planning/phases/53-series-optimization/53-01-SUMMARY.md`
</output>
