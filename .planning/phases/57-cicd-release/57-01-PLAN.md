---
phase: 57-cicd-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - Makefile
autonomous: true

must_haves:
  truths:
    - "Pushing a v* tag triggers GitHub Actions workflow"
    - "Workflow produces Linux x86_64 (static) and macOS arm64 (dynamic) binaries"
    - "GitHub Release created with binaries and SHA256 checksums"
    - "macOS build omits -static flag"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered release workflow"
      contains: "softprops/action-gh-release"
---

<objective>
Create a GitHub Actions release workflow that:
1. Triggers on v* tag push
2. Builds Linux x86_64 (static) and macOS arm64 (dynamic) binaries
3. Generates SHA256 checksums
4. Creates GitHub Release with all assets attached

Output: `.github/workflows/release.yml`
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create release workflow</name>
  <files>.github/workflows/release.yml</files>
  <action>
Create GitHub Actions workflow with:
- Trigger: push tags v*
- Matrix: linux (ubuntu-24.04, static), macos (macos-14, no -static, arm64)
- Build step: g++ -std=c++20 -O2 -Wall -Wextra [-static on Linux] -o qseries src/main.cpp
- Rename binaries: qseries-linux-amd64, qseries-darwin-arm64
- Run acceptance tests on Linux
- Generate SHA256 checksums file
- Upload all assets to GitHub Release via softprops/action-gh-release@v2
  </action>
  <done>Release workflow created</done>
</task>

</tasks>

<verification>
1. Workflow YAML is valid
2. Matrix covers Linux x86_64 and macOS arm64
3. macOS build omits -static
4. Checksums generated
5. softprops/action-gh-release creates release with assets
</verification>
