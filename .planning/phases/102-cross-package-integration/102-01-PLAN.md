---
phase: 102-cross-package-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration-eta-theta-modforms.sh
  - tests/run-all.sh
autonomous: true
user_setup: []

must_haves:
  truths:
    - "integration-eta-theta-modforms.sh passes all tests including single proof chain"
    - "At least one test chains eta identity + theta ID + modular form basis in one flow"
    - "integration-eta-theta-modforms.sh is included in run-all.sh"
    - "Script runs in minimal PATH (grep available)"
  artifacts:
    - path: tests/integration-eta-theta-modforms.sh
      provides: Cross-package integration tests
      contains: "makeALTbasisM|makebasisM"
    - path: tests/run-all.sh
      provides: Run-all test orchestration
      contains: "integration-eta-theta-modforms"
  key_links:
    - from: "tests/integration-eta-theta-modforms.sh"
      to: "provemodfuncGAMMA0id"
      via: check/run invocation
      pattern: "provemodfuncGAMMA0id"
    - from: "tests/integration-eta-theta-modforms.sh"
      to: "jac2eprod|etamake"
      via: theta→eta chain
      pattern: "jac2eprod|etamake"
    - from: "tests/run-all.sh"
      to: "integration-eta-theta-modforms.sh"
      via: for loop entry
      pattern: "integration-eta-theta-modforms"
---

<objective>

Ensure ETA identity prover, theta IDs, and modular forms work together in a single proof chain; integration script passes robustly and is included in run-all.

Purpose: Satisfy INT-02 strict interpretation — one end-to-end flow using eta + theta + modular form basis together.
Output: Updated integration-eta-theta-modforms.sh with single-chain test, PATH hardening, run-all.sh including the script.
</objective>

<execution_context>

@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md

</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-cross-package-integration/102-CONTEXT.md
@.planning/phases/102-cross-package-integration/102-RESEARCH.md

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add single proof chain test</name>
  <files>tests/integration-eta-theta-modforms.sh</files>
  <action>

Add a new test that chains eta identity + theta ID + modular form basis in one flow. Per CONTEXT: use makebasisM or makeALTbasisM (not just DELTA12).

Implementation: Add a sixth test that runs a sequence of commands in one pipe:
1. Build modular form basis: B := makeALTbasisM(12, 50) (or makebasisM(12, 50))
2. Express DELTA12 in basis: findlincombo(DELTA12(50), makeALTbasisM(12,50), 0) — verifies DELTA12 lies in span (modforms)
3. Eta identity: provemodfuncGAMMA0id([[1, 2,24, 1,-24], [-1, 2,24, 1,-24]], 2) — proven=1 (eta)
4. Theta→eta: etamake(theta3(50), 50) — yields eta product (theta IDs via convert)

Run as a single multi-line piped invocation. Use the existing check() or run() pattern. Grep for success: e.g. "proven=1" and GETA/η/\[\[ from etamake, and non-empty/finite output from findlincombo. The test must invoke makeALTbasisM or makebasisM explicitly — DELTA12 alone does not satisfy "modular form basis" per user decision.

Alternative simpler chain if findlincombo on basis is complex: One run() that (a) computes makeALTbasisM(12,50) and checks output is non-empty, (b) runs provemodfuncGAMMA0id, (c) runs etamake(theta3(50),50). All in one pipe; grep for success indicators. The key: makeALTbasisM/makebasisM must be called in the chain.
  </action>
  <verify>bash tests/integration-eta-theta-modforms.sh 2>&1 | tail -5 shows "6 passed, 0 failed" (or higher pass count including new test)</verify>
  <done>New test exists, uses makeALTbasisM or makebasisM, and passes when script runs</done>
</task>

<task type="auto">
  <name>Task 2: Harden integration script for minimal PATH</name>
  <files>tests/integration-eta-theta-modforms.sh</files>
  <action>

Ensure the integration script works when run in a minimal PATH (e.g. Cygwin where grep may not be in default PATH). Per CONTEXT and RESEARCH: fix by ensuring test runners use a shell with standard Unix tools.

Add near the top of the script (after shebang, before BIN discovery):
  export PATH="/usr/bin:$PATH"

This ensures grep, tail, etc. are found when the script runs under minimal PATH. Do not break existing behavior — only add the export.
  </action>
  <verify>Run bash tests/integration-eta-theta-modforms.sh with PATH=/tmp (or empty) — script should still pass if /usr/bin has grep; or run normally and confirm no regression</verify>
  <done>Script includes PATH export; grep and other standard tools are available when script runs</done>
</task>

<task type="auto">
  <name>Task 3: Include integration script in run-all.sh</name>
  <files>tests/run-all.sh</files>
  <action>

Add tests/integration-eta-theta-modforms.sh to the for-loop in tests/run-all.sh. Per CONTEXT: "include in run-all if missing" — RESEARCH confirms it is not currently included.

Append "tests/integration-eta-theta-modforms.sh" to the space-separated list in the for t in ... line. Preserve existing order; add at end of list.
  </action>
  <verify>grep -q "integration-eta-theta-modforms" tests/run-all.sh</verify>
  <done>run-all.sh runs integration-eta-theta-modforms.sh as part of the suite</done>
</task>

</tasks>

<verification>

1. bash tests/integration-eta-theta-modforms.sh — all tests pass (6+ passed, 0 failed)
2. bash tests/run-all.sh — completes; integration-eta-theta-modforms output appears
3. make acceptance-all — passes (no regressions)
4. bash tests/maple-checklist.sh — passes (no regressions)
</verification>

<success_criteria>

- integration-eta-theta-modforms.sh passes with single proof chain test using makeALTbasisM or makebasisM
- run-all.sh includes integration-eta-theta-modforms.sh
- No regressions in maple-checklist or acceptance-all
</success_criteria>

<output>

After completion, create `.planning/phases/102-cross-package-integration/102-01-SUMMARY.md`
</output>
