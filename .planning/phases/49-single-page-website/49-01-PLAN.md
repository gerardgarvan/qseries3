---
phase: 49-single-page-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "A single self-contained HTML file exists at repo root with kangaroo-banner hero section"
    - "Build instructions section covers prerequisites, compile command, and platform notes"
    - "Key examples section shows Rogers-Ramanujan, partition function, theta, relations with REPL I/O"
    - "Full function reference documents all 50+ built-in functions with signatures and descriptions"
    - "Design uses kangaroo aesthetic: dark #0d0617 background, sunset gradient, gold #ffe066, Bebas Neue + Outfit fonts"
  artifacts:
    - path: "index.html"
      provides: "Complete single-page website"
      contains: "KANGAROO"
  key_links:
    - from: "index.html"
      to: "MANUAL.md"
      via: "Function reference content derived from MANUAL.md built-in documentation"
      pattern: "prodmake"
---

<objective>
Create a single comprehensive HTML page at the repo root (index.html) that serves as the project's web presence. Uses the kangaroo-banner aesthetic from C:\Users\ggarv\Downloads\kangaroo-banner.html as the hero section and design language. Content includes project overview, build instructions, key examples with REPL I/O, and a full 50+ function reference.

Purpose: Replace the deleted Astro Starlight website with a single beautiful page.
Output: `index.html` — self-contained, no external JS dependencies, only Google Fonts.
</objective>

<execution_context>
@C:/Users/ggarv/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ggarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@MANUAL.md
@C:\Users\ggarv\Downloads\kangaroo-banner.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with hero, navigation, and build instructions</name>
  <files>index.html</files>
  <action>
Create `index.html` at the repository root. This is a single self-contained HTML file.

**Design system** (from kangaroo-banner.html):
- Background: `#0d0617` (deep purple-black)
- Sunset gradient: `#1a0a2e → #3d1155 → #8b2a2a → #d4451a → #e8762b → #f7a634 → #fcd980`
- Accent/gold: `#ffe066`
- Dust color: `#e8c47c`
- Ground: `#2a1508 → #1a0d04`
- Fonts: `'Bebas Neue'` for headings, `'Outfit'` for body text (Google Fonts)
- Monospace for code: system monospace stack

**Structure:**

1. **Hero section** — Port the full kangaroo-banner.html as the page hero:
   - Sunset sky gradient background (full viewport width, 400px height)
   - Stars (radial-gradient dots with twinkle animation)
   - Sun (radial gradient with pulse animation)
   - Ground (dark brown with clip-path hills)
   - Dust particles (blurred, drifting)
   - Kangaroo SVG silhouette (hopping animation)
   - Kangaroo shadow (squishing animation)
   - Boomerang SVG (spin-float animation)
   - Scanlines overlay
   - Title: "KANGAROO" in Bebas Neue gold, subtitle "q-series research tool" in Outfit
   - Accent line (animated grow)

2. **Sticky navigation bar** (below hero, sticks to top on scroll):
   - Dark background with subtle border
   - Links: Overview | Build | Examples | Reference
   - Smooth scroll to sections

3. **Overview section:**
   - Brief description: "A standalone single-binary REPL for q-series computation..."
   - Key features as a grid: Exact Arithmetic, Andrews' Algorithm, Relation Finding, 50+ Built-ins
   - Attribution to Frank Garvan's qseries Maple package

4. **Build section:**
   - Prerequisites: C++20 compiler (g++ 10+, clang 13+)
   - One-liner: `g++ -std=c++20 -O2 -static -o qseries src/main.cpp`
   - Platform notes for Linux, macOS, Windows (MSYS2/Cygwin)
   - Quick verification: `echo "version" | ./qseries`

**Styling rules:**
- All CSS inline in `<style>` tag — no external CSS files
- Section backgrounds alternate between `#0d0617` and `#0f0820` for visual rhythm
- Gold `#ffe066` for section headings (Bebas Neue)
- Body text in `rgba(255,224,102,0.85)` (Outfit light)
- Code blocks use dark background `#1a0e2e` with `#e8c47c` text
- REPL prompts in gold, output in dust color
- Responsive: max-width container (900px), works on mobile
- Smooth scroll behavior on html element
  </action>
  <verify>
1. `index.html` exists at repo root
2. File opens in browser showing kangaroo banner hero
3. Navigation links work (smooth scroll)
4. Build instructions section is present with compile command
  </verify>
  <done>index.html created with hero, nav, overview, and build sections</done>
</task>

<task type="auto">
  <name>Task 2: Add examples and full function reference</name>
  <files>index.html</files>
  <action>
Append two major content sections to index.html:

**5. Examples section** — 4 showcase examples with REPL input/output blocks:

  a) **Rogers-Ramanujan Identity:**
  ```
  qseries> G := sum(q^(n^2)/aqprod(q,q,n,50), n, 0, 8)
  [series output]
  qseries> prodmake(G, 50)
  [product output showing denominators at ±1 mod 5]
  ```

  b) **Partition Function:**
  ```
  qseries> f := 1/etaq(1,30)
  [series with partition numbers as coefficients]
  qseries> partition(100)
  190569292
  ```

  c) **Theta Functions:**
  ```
  qseries> etamake(theta3(50), 50)
  [eta product identification]
  ```

  d) **Finding Relations (Gauss AGM):**
  ```
  qseries> a := theta3(50)^2;
  qseries> b := theta4(50)^2;
  qseries> c := theta2(50)^2;
  qseries> d := theta3(50)*theta4(50);
  qseries> findhom([a,b,c,d], 2, 0)
  [relations output]
  ```

  **IMPORTANT:** Run each example through the actual qseries binary to get real output. Use:
  ```bash
  echo 'COMMAND' | ./qseries.exe 2>/dev/null | tail -n +2
  ```
  to capture the actual output (skip the banner line). Use this real output in the HTML.

  Style each example as a dark terminal-like block with:
  - Title in Bebas Neue gold
  - Brief math explanation in Outfit
  - REPL block with `qseries>` prompt in gold, output in dust color
  - Monospace font for all code

**6. Function Reference section** — All 53 built-in functions organized in categories:

  Read the help table from src/repl.h (lines 150-203) for the complete list. Organize into groups:

  - **Series Construction:** aqprod, etaq, qbin, T, theta, theta2, theta3, theta4, tripleprod, quinprod, winquist, eisenstein
  - **Series Operations:** series, coeffs, coeff, qdegree, lqdegree, subs_q, qdiff, sift, dissect
  - **Product Conversion:** prodmake, mprodmake, etamake, jacprodmake, qfactor, jac2prod, jac2series, checkprod, checkmult
  - **Relation Finding:** findhom, findnonhom, findhomcombo, findnonhomcombo, findpoly, findlincombo, findmaxind
  - **Number Theory:** legendre, jacobi, kronecker, sigma, divisors, mobius, euler_phi, partition
  - **Summation:** sum, add
  - **REPL Commands:** set_trunc, version, help, clear_cache, min, max

  For each function, show:
  - Function name in gold monospace
  - Signature (from help table)
  - One-line description

  Style as a compact reference grid/table — not one-per-row (too long), but grouped cards or a dense table.

**7. Footer:**
  - "Based on Frank Garvan's qseries Maple package"
  - GitHub link placeholder
  - Subtle sunset gradient border at bottom
  </action>
  <verify>
1. Examples section has 4 working examples with real REPL output
2. Function reference lists all 53 built-ins organized by category
3. Page renders correctly in browser — all sections visible, styled consistently
4. Footer exists with attribution
  </verify>
  <done>Complete single-page website with examples and function reference</done>
</task>

</tasks>

<verification>
1. `index.html` exists at repo root
2. Hero section has kangaroo banner with all animations (stars, sun, kangaroo, boomerang, dust)
3. Navigation bar with smooth scroll works
4. Build instructions section has compile one-liner and platform notes
5. 4 examples with real REPL output (Rogers-Ramanujan, partition, theta, relations)
6. Function reference covers all 53 built-ins in organized categories
7. Consistent kangaroo aesthetic: dark background, gold headings, sunset accents
8. Page is self-contained (no external JS, only Google Fonts CSS)
</verification>

<success_criteria>
- Single HTML file, beautiful, informative, self-contained
- All content accurate and matching actual REPL behavior
- Kangaroo-banner aesthetic consistently applied throughout
</success_criteria>

<output>
After completion, create `.planning/phases/49-single-page-website/49-01-SUMMARY.md`
</output>
